# دفتر تغییرات راوی

در این دفتر، نتیجهٔ هر تغییر را به زبان ساده می‌خوانید. سندهای راوی فایل‌های
استاندارد Markdown هستند و نظرها و نشانه‌ها نیز به‌صورت قابل‌حمل کنار همان متن می‌مانند.

## 2.4.5 — ۱۵ شهریور ۱۴۰۵

- پنجرهٔ نوشتن نظر در مرکز صفحه باز می‌شود؛ پیمایش با صفحه‌کلید، بستن با Escape،
  بازگشت تمرکز و بازیابی پیش‌نویس آن اصلاح شده‌اند.
- ذخیرهٔ فایل در برابر خطای نوشتن و قطع عملیات مقاوم‌تر شده و تغییرات هم‌زمان
  به‌ترتیب ذخیره می‌شوند.
- اجرای Mermaid از پنجرهٔ اصلی جدا شده است؛ نمودار متوقف‌شده دیگر نباید کل برنامه
  را از پاسخ‌گویی بیندازد و مسیر بازیابی پس از خطای پردازش اضافه شده است.
- جست‌وجو و فهرست فصل‌ها در سندهای بلند، حفظ جای مطالعه و بستن انتخاب متن اصلاح شدند.
- در استودیو فرمول، تایپ سریع و Tab دیگر تمرکز را به خانهٔ قبلی برنمی‌گرداند.
- دانلود به‌روزرسان محدودیت حجم و زمان دارد و فایل دریافت‌شده با SHA-512 و Ed25519
  بررسی می‌شود. وابستگی‌ها و فایل قفل نیز یکسان شده‌اند.

- موتور `Persian IPA F5-TTS` همراه مبدل محلی فارسی‌به‌IPA و Vocos آفلاین به
  تنظیمات شنیدن افزوده شد و محدودیت مجوز غیرتجاری آن به‌روشنی نمایش داده می‌شود.
- متن‌های بلند پیش از آوانویسی و ساخت صدا در مرز طبیعی تقسیم می‌شوند؛ تبدیل IPA در
  حافظه کش می‌شود و پنج قطعهٔ بعدی برای خوانش پیوسته از پیش آماده می‌مانند.
- مسیر نصب و داده‌های راوی در ویندوز کاملاً انگلیسی شد و داده‌های مسیر قدیمی بدون
  دانلود دوباره به `%APPDATA%\\Raavi` مهاجرت می‌کنند.
- مشکل نصب گویا روی مسیرهای فارسی Windows/SentencePiece، قفل کاذب «دانلود موتور
  دیگری در حال انجام است» و نمایش متن خام خطای Electron برطرف شد.

وضعیت آزمون این نامزد: ۴۶۱ آزمون واحد و ۲۳۸ آزمون عملکردی موفق؛ آزمون اختصاصی R10
به فایل ورودیِ فراهم‌نشده نیاز دارد. انتقال سند و نظر از 2.2.1 به 2.4.5 بررسی شده
است. Windows فاقد امضای Authenticode است؛ امضای Ed25519 به‌روزرسان، امضای Windows
نیست. نصب و ارتقای واقعی با نصب‌کننده روی Windows آزمایشی دارای دسترسی مدیر هنوز
بررسی نشده‌اند. برای این نسخه، اعتبارسنجی نمای موبایل انجام نشده است.

## 2.4.4 — ۱۱ شهریور ۱۴۰۵

- موتور `Gooya V1` با مجوز اختصاصی آوا صبای ملل، G2P فارسی Negara و کُدک MOSS
  به‌عنوان گزینهٔ محلی تازه به تنظیمات شنیدن افزوده شد.
- بستهٔ کامل مدل و کتابخانه‌های اجرا با checksum از هاست راوی دریافت می‌شود و پس از
  نصب، آوانویسی و ساخت صدا بدون اینترنت انجام می‌گیرد.
- برای جلوگیری از جاافتادن پایان جمله، بودجهٔ متن Gooya محافظه‌کارانه است و خروجی
  ناقص به‌صورت خودکار به قطعات کوچک‌تر تقسیم و بازتولید می‌شود.
- سرعت Gooya در پخش‌کننده اعمال می‌شود تا زیر و بَم صدا حفظ شود؛ پنج قطعهٔ بعدی نیز
  در بافر و کش ماندگار آماده می‌شوند.

## 2.4.3 — ۱۰ شهریور ۱۴۰۵

- آوا اکنون متن فارسی را پیش از ساخت صدا به‌صورت اجباری پاک‌سازی و در مرزهای طبیعی
  به قطعه‌های امن تقسیم می‌کند؛ جمله‌های بلند دیگر یک‌باره به موتور سپرده نمی‌شوند.
- بافر ۳ تا ۵ قطعه‌ای و کش ماندگار صوت اضافه شد تا شروع‌های بعدی سریع‌تر و ادامهٔ
  خواندن روان‌تر باشد؛ وضعیت آماده‌سازی در تولبار معلق پایین صفحه دیده می‌شود.
- خروجی صوتیِ کوتاه یا ناقص با مدت، نرخ نمونه و تعداد فریم سنجیده می‌شود و راوی آن
  بخش را خودکار کوچک‌تر می‌کند، دوباره می‌سازد و بدون حذف به هم می‌چسباند.
- «کارگردان هوشمند فارسی» به‌صورت اختیاری از اتصال ChatGPT/Codex راوی برای تلفظ،
  اعراب و مکث بهتر استفاده می‌کند؛ اگر اتصال در دسترس نباشد خوانش محلی ادامه می‌یابد.

## 2.4.2 — ۱۰ شهریور ۱۴۰۵

- مدل فارسی `Meta MMS` به‌عنوان موتور سومِ کاملاً محلی برای استفادهٔ شخصی، آموزشی
  و پژوهشیِ غیرتجاری اضافه شد و همهٔ فایل‌ها و نیازمندی‌های آن از هاست راوی دریافت می‌شوند.
- حجم هر موتور اکنون به فایل اصلی مدل، فایل‌های همراه، کتابخانه‌های اجرا و Python
  مشترک تفکیک می‌شود و مقدار واقعی دانلود موردنیاز همان رایانه نمایش داده می‌شود.
- خطاهای خام شبکه، سرور و Python با پیام فارسی و راه بازیابی مناسب جایگزین شدند؛
  وضعیت آغاز دانلود نیز به‌جای صفر درصد، مرحلهٔ برقراری اتصال را نشان می‌دهد.

## 2.4.1 — ۱۰ شهریور ۱۴۰۵

- نصب دوبارهٔ موتورهای شنیدن پس از توقف یا خطای میانی، محیط Python نیمه‌کاره را
  به‌صورت امن بازسازی می‌کند و دیگر روی پیام «محیط مجازی از قبل وجود دارد» نمی‌ماند.
- مرحلهٔ نصب وابستگی‌های محلی اکنون با پیام روشن‌تری نمایش داده می‌شود تا با توقف
  دانلود اشتباه گرفته نشود.
- Python 3.11 و runtimeهای کامل Piper، Torch، Kokoro و آوا نیز از هاست دانلود راوی
  با checksum دریافت می‌شوند؛ نصب شنیدن دیگر به PyPI یا GitHub وابسته نیست.

## 2.4.0 — ۱۰ شهریور ۱۴۰۵

- قابلیت «شنیدن متن» به حالت مطالعه اضافه شد؛ خواندن از ابتدای سند آغاز می‌شود و
  جملهٔ جاری با رنگ آبی و پاراگراف فعال با نشانهٔ حاشیه‌ای مشخص می‌ماند.
- تولبار معلق پایین‌ـ‌وسط برای پخش، مکث، توقف، تغییر سرعت و رفتن به پاراگراف قبل یا
  بعد اضافه شد؛ کلیدهای بالا و پایین نیز همین جابه‌جایی را انجام می‌دهند.
- دو موتور فارسی محلی مانا و آوا با دریافت قابل ادامه، شنیدن نمونه، انتخاب، حذف و
  کنترل نسخه و SHA-256 در تنظیمات مطالعه در دسترس‌اند.
- وزن‌های مدل از هاست دانلود راوی دریافت می‌شوند و پس از نصب، ساخت صدا کاملاً روی
  همان رایانه انجام می‌شود.

## 2.3.0 — ۷ شهریور ۱۴۰۵

- انتخاب متن، دوبارکلیک، درگ و جابه‌جایی خط‌به‌خط در نماهای نوشتن، کد و مطالعه
  پایدارتر شد؛ تمرکز جدول دیگر سند را به ابتدای صفحه نمی‌پراند.
- کد خام Mermaid و فرمول در نمای نوشتن پنهان می‌ماند و فقط نمای باکس‌دار قابل‌ویرایش
  نمایش داده می‌شود؛ نمودارهای Mermaid در اسناد بزرگ نیز پایدارتر رندر می‌شوند.
- پیمایش با فلش بالا و پایین هیچ خطی را جا نمی‌اندازد و بلاک فعال را داخل محدودهٔ امن
  دید نگه می‌دارد.
- First Run و تنظیمات با تصاویر محلی سازگار با Worker و وضعیت‌های اتصال واقعی‌تر
  بهبود یافتند.
- همهٔ هشدارهای قدیمی lint، وابستگی‌های React Hooks و هشدارهای تکراری گیت ساخت پاک
  شدند؛ گیت و بودجه‌های باندل بدون خطا پاس می‌شوند.

## 2.2.2 — ۶ شهریور ۱۴۰۵

- اجرای نصب‌کنندهٔ بروزرسانی در Windows از مسیر Windows Shell انجام می‌شود تا
  درخواست دسترسی مدیر سیستم (UAC) برای نصب سراسری به‌درستی نمایش داده شود.
- شکست یا لغو اجرای نصب‌کننده دیگر باعث خطای JavaScript در پردازش اصلی و بسته‌شدن
  ناخواستهٔ راوی نمی‌شود؛ کاربر می‌تواند دوباره تلاش کند یا دانلود مستقیم را باز کند.
- راوی فقط پس از بازشدن موفق نصب‌کننده بسته می‌شود و خطای `EACCES` با تست رگرسیون
  اختصاصی پوشش داده شده است.

## 2.2.1 — ۶ شهریور ۱۴۰۵

- آپدیتر امن به manifest چندسکویی ارتقا یافت؛ Windows، Apple Silicon و Intel
  artifact بومی خود را انتخاب می‌کنند، دانلود قابل ادامه و mirror جایگزین دارند و
  هر فایل پیش از نصب با اندازه، SHA-512 و امضای Ed25519 مستقل اعتبارسنجی می‌شود.
- در macOS بررسی نسخه، دانلود پس‌زمینه، مکث و ادامه و آماده‌سازی DMG اکنون از داخل
  خود راوی انجام می‌شود؛ نصب نهایی با تأیید کاربر در نصب‌کنندهٔ استاندارد macOS
  ادامه پیدا می‌کند.
- manifest نسخهٔ ۲ برای سازگاری با نسخه‌های قبلی، artifact قدیمی Windows را نیز
  نگه می‌دارد و انتشار فقط پس از آماده‌بودن هر سه خروجی فعال می‌شود.
- در ویرایش روان، کد معمولی و فراخوان دیگر داخل کارت جداگانه قرار نمی‌گیرند و
  مستقیماً در متن قابل ویرایش‌اند؛ فقط عکس، جدول، فرمول، Mermaid و صوت نمای
  باکس‌دار دارند و کلیک روی کد دیگر مکان ویرایش را به خط بعدی نمی‌پراند.

## 2.2.0 — ۶ شهریور ۱۴۰۵

- خروجی‌های بومی macOS برای Apple Silicon و Intel به زیرساخت GitHub Actions و همان GitHub Release اضافه شدند؛ هر معماری DMG، ZIP، گزارش ساخت و SHA-256 مستقل دارد و انتشار unsigned فقط با تأیید دستی و نام‌گذاری شفاف مجاز است.
- بروزرسانی داخلی امن به راوی اضافه شد؛ برنامه نسخهٔ تازه را پیشنهاد می‌دهد، دانلود را در پس‌زمینه با امکان مکث و ادامه انجام می‌دهد و فقط Installer دارای اندازه، SHA-512 و امضای Ed25519 معتبر را آمادهٔ نصب می‌کند.
- فایل‌های حجیم از هاست دانلود نسخه‌بندی‌شده دریافت می‌شوند و manifest و یادداشت نسخه روی دامنهٔ اصلی باقی می‌مانند؛ معماری mirror امکان جابه‌جایی یا افزودن هاست را بدون تغییر برنامه فراهم می‌کند.
- راه‌اندازی سه‌مرحله‌ای برای انتخاب مخزن، آماده‌سازی ChatGPT/Codex CLI و اتصال Google Drive یا Proton Drive تکمیل شد؛ مسیر مخزن و اتصال‌های موجود از قبل شناسایی می‌شوند و کاربر اطلاعات تکراری وارد نمی‌کند.
- نصب‌نبودن Codex CLI با فرمان روشن، کپی، راهنما، اجرای مستقیم در PowerShell و بررسی دوباره مدیریت می‌شود؛ رنگ و میکروکپی این مرحله برای تجربهٔ اولین ورود آرام‌تر شده‌اند.
- پشتیبان‌گیری و بازیابی مخزن برای Google Drive و Proton Drive، انتخاب نوع محتوا، فشرده‌سازی رسانه و نگه‌داری نسخهٔ نهایی تکمیل شد؛ فایل‌های صرفاً مطالعاتی تا زمان ویرایش یا انتقال به مخزن وارد بکاپ نمی‌شوند.
- تنظیمات فشرده‌تر و خواناتر شد، اجرای دوبارهٔ خوش‌آمدگویی و کنترل بروزرسانی در برگهٔ عمومی قرار گرفت و بنر وضعیت دانلود در پایین راست بدون توقف کار کاربر نمایش داده می‌شود.
- حرکت صفحه‌کلیدی میان بلاک‌ها اکنون خط یا بلاک فعال را داخل محدودهٔ امن دید نگه می‌دارد؛ بلاک «جداکننده»، میانبر آن و کپی و جای‌گذاری در مینی‌منوی ویرایش نیز کامل شدند.
- انتخاب متن با کشیدن نشانگر در نمای مطالعه دوباره دیدنی و قابل‌اعتماد است و پس از رهاکردن نشانگر، ابزارهای متن بدون ازدست‌رفتن محدودهٔ انتخاب‌شده باز می‌شوند.
- نمودارهای Mermaid در نمای مطالعه تم فعال را حفظ می‌کنند و فضای نمایش بلندتر و خواناتری دارند؛ حالت آسان استودیوی نمودار نیز در اندازه‌های دسکتاپ و فشرده خلوت‌تر و بدون هم‌پوشانی شده است.
- واژگان رابط یکدست شد و در نمای مطالعه، راه‌اندازی و تنظیمات فقط از نام «مخزن» استفاده می‌شود.

## 2.1.13 — ۵ شهریور ۱۴۰۵

- بلاک «جداکننده» به فهرست بلاک‌ها و منوی اسلش اضافه شد و در سند به‌صورت Markdown استاندارد `---` ذخیره می‌شود.
- فرمان «درج جداکننده» با میانبر `Alt+Shift+H` در فرمان‌خانه و تنظیمات میانبرها ثبت شد و از هر دو مسیر قابل اجراست.
- کپی و جای‌گذاری در مینی‌منوی ویرایش در دسترس‌اند و گروه‌های ابزار با جداکنندهٔ دیداری خواناتر شده‌اند.

## 2.1.12 — ۵ شهریور ۱۴۰۵

- نمایش خودکار First Run دیگر به شمارهٔ نسخه یا نصب تازه وابسته نیست و پس از به‌روزرسانی نیز وضعیت واقعی سرویس‌ها را بررسی می‌کند.
- اگر ChatGPT/Codex یا هر دو سرویس Google Drive و Proton Drive کامل متصل نباشند، راه‌اندازی دوباره نمایش داده می‌شود.
- وقتی ChatGPT و دست‌کم یکی از سرویس‌های بکاپ متصل باشند، راه‌اندازی خودکار نمایش داده نمی‌شود؛ اجرای دستی آن از تنظیمات همچنان در دسترس است.

## 2.1.11 — ۴ شهریور ۱۴۰۵

- بازیابی بکاپ‌های Google Drive و Proton Drive به تنظیمات اضافه شد؛ اسناد متنی به‌صورت پیش‌فرض انتخاب می‌شوند و کاربر مقصد را تعیین می‌کند.
- هر بازیابی در یک پوشهٔ تازه انجام می‌شود و هیچ فایل موجودی بازنویسی نمی‌شود؛ سندها همچنان Markdown استاندارد هستند و تصویر، صوت، پیوست، نظرها و تاریخچهٔ موجود نیز برمی‌گردند.
- پوشهٔ بازیابی به کتابخانهٔ راوی اضافه می‌شود و نتیجهٔ موفق یا ناموفق هر سند به‌صورت روشن نمایش داده می‌شود.

## 2.1.10 — ۴ شهریور ۱۴۰۵

- خطای دسترسی‌نداشتن شبکه به سرورهای Proton از قفل داخلی CLI تفکیک شد و پیام دقیق‌تری در تنظیمات نمایش داده می‌شود.
- راوی دیگر عملیات ورود کند Proton را پس از ۱۵ ثانیه به‌اشتباه «اجرای بازمانده» تشخیص نمی‌دهد و اجازه می‌دهد خود CLI نتیجهٔ واقعی شبکه را گزارش کند.

## 2.1.9 — ۴ شهریور ۱۴۰۵

- لینک ورود مرورگری Proton اکنون از خروجی رسمی CLI خوانده و مستقیماً با مرورگر پیش‌فرض باز می‌شود؛ بازنشدن خودکار مرورگر در Windows دیگر اتصال را متوقف نمی‌کند.
- اگر اجرای دیگری از Proton Drive CLI قفل داخلی را نگه داشته باشد، اتصال پس از زمان کوتاه با پیام روشن متوقف می‌شود و در حالت «در حال اتصال» باقی نمی‌ماند.

## 2.1.8 — ۴ شهریور ۱۴۰۵

- Proton Drive در کنار Google Drive به پشتیبان‌گیری ابری مخزن اضافه شد و کاربر می‌تواند سرویس را از همان صفحهٔ تنظیمات انتخاب کند.
- ورود Proton در مرورگر رسمی انجام می‌شود؛ راوی رمز عبور را دریافت نمی‌کند و فقط از Proton Drive CLI رسمی و اعتبارسنجی‌شده استفاده می‌کند.
- با تغییر سرویس، نسخهٔ جاری فایل‌های مخزن دوباره در صف قرار می‌گیرد؛ کش مطالعه همچنان خارج از بکاپ می‌ماند و سیاست متن، تصویر، صوت، پیوست و تاریخچه بدون تغییر اعمال می‌شود.
- هر سند در Proton به‌صورت پوشهٔ قابل‌جایگزینی ذخیره می‌شود تا نسخه‌ها و رسانه‌های قدیمی پس از اعمال سیاست نگه‌داری روی فضای ابری انباشته نشوند.

## 2.1.4 — ۳ شهریور ۱۴۰۵

- اتصال هوش مصنوعی راوی به یک مسیر روشن ChatGPT ساده شد؛ ورود، بررسی اتصال و «قطع و اتصال دوباره» در تنظیمات باقی مانده‌اند.
- فهرست مدل‌های قابل استفاده مستقیماً از حساب متصل دریافت می‌شود؛ ورودی متنی آزاد حذف شده و مدل انتخابی با نام روشن نمایش و ذخیره می‌شود.
- چت، بازنویسی، بررسی فارسی، نشانه‌گذاری و پاک‌سازی متن صوت همگی از مدل انتخاب‌شدهٔ ChatGPT استفاده می‌کنند.
- کدهای تشخیص، ورود، آزمون و اجرای Claude، Gemini، Cursor و GitHub Copilot و تست‌های اختصاصی آن‌ها از نسخهٔ جاری حذف شدند.

## 2.1.3 — ۳ شهریور ۱۴۰۵

- برای هر موتور هوش مصنوعی دکمهٔ «قطع و بازنشانی» اضافه شد تا کاربر بتواند اطلاعات ورود همان CLI را با تأیید صریح پاک کند، دوباره وارد شود و اتصال را از نو بیازماید.
- بازنشانی به اطلاعات ورود همان موتور محدود است و فایل‌ها، مدل‌های گفتاری و سایر تنظیمات را حذف نمی‌کند.
- تشخیص Cursor، راهنمای نصب Gemini و آزمون و گزارش محدودیت دسترسی GitHub Copilot اصلاح شد.

## 2.0.0 — ۲ شهریور ۱۴۰۵

- استودیوی نمودار با ۲۹ نوع Mermaid، جست‌وجوی فارسی و انگلیسی، تصویر نمونهٔ واقعی و پیش‌نمایش زندهٔ سمت چپ بازطراحی شد؛ هاور پیش‌نمایش را عوض می‌کند و انتخاب مستقیم به ساخت می‌رود.
- مسیرهای ساخت آسان، کد پیشرفته و راوی هوشمند به هم متصل شدند تا کاربر در هر مرحله بتواند بدون ازدست‌دادن نمودار میان آن‌ها جابه‌جا شود.
- انیمیشن شش‌ثانیه‌ای تأییدشدهٔ فیگما، با موج نور، گره‌های شیشه‌ای و میکروکپی مرحله‌ای، داخل خود بوم پیش‌نمایش زنده پیاده شد.
- تشخیص نوع نمودار اکنون directive و کامنت ابتدایی Mermaid را رد می‌کند؛ نمودارهایی مثل Pie با `%%{init...}%%` در سندهای موجود دوباره درست رندر و ویرایش می‌شوند.
- فرمان‌خانهٔ هوشمند با ۴۷ فرمان نوشتاری، بررسی پیشرفتهٔ فارسی و نشانه‌گذاری هوشمند متصل به کامنت‌ها تکمیل شد.
- فایل استاندارد `.md` و `.markdown` مسیر اصلی ذخیره است و وابستگی رابط به فرمت اختصاصی `.ravi` حذف شد.
- Electron مسیر رسمی انتشار Windows شد؛ نصاب، آیکون شفاف برنامه، آیکون اختصاصی فایل Markdown و گیت‌های Upgrade/Uninstall به‌روزرسانی شدند.

## 1.5.2 — ۱۷ مرداد ۱۴۰۵

- پس از جمع‌کردن فهرست فصل‌ها در حالت مطالعه، برگ خواندن اکنون دقیقاً در مرکز فضای مطالعه قرار می‌گیرد.
- لندینگ در صورت مسدود یا گم‌شدن `landing.md` به نمونهٔ داخلی سالم برمی‌گردد و ویرایش و پیش‌نمایش را فعال نگه می‌دارد.
- نسخهٔ وب، صفحهٔ معرفی، بستهٔ cPanel و خروجی Windows با اطلاعات انتشار 1.5.2 تازه می‌شوند.

## 1.5.0 — ۱۴ مرداد ۱۴۰۵

- خروجی Word و PDF از پیش‌نمایش خواندن به راوی افزوده شد تا نسخهٔ تحویلی تا حد ممکن با محتوای دیده‌شده در برنامه همسان باشد.
- PDF در یک سند چاپی مستقل ساخته می‌شود؛ همهٔ اجزای رابط کاربری حذف شده‌اند و نسخهٔ دسکتاپ سربرگ و پابرگ مرورگر را چاپ نمی‌کند.
- سند Word با صفحهٔ A4 و حاشیه‌های عددی استاندارد ساخته می‌شود، ساختار عنوان‌ها، جدول‌ها و فهرست‌ها قابل‌ویرایش می‌ماند و frontmatter به مشخصات سند منتقل می‌شود.
- نمودارهای Mermaid در Word با SVG برداری و PNG جایگزین ۴۸۰ DPI، رزولوشن تطبیقی و حاشیهٔ امن صادر می‌شوند تا نوشته‌های نمودار خواناتر بمانند.
- اگر آماده‌سازی هر نمودار Mermaid شکست بخورد، ساخت خروجی تا تأیید صریح کاربر ادامه پیدا نمی‌کند.
- مسیرهای ذخیرهٔ Word و PDF در نسخه‌های دسکتاپ Electron و Tauri تکمیل و خطاهای ذخیره‌سازی با پیام روشن مدیریت شدند.
- آزمون پذیرش سند سنگین فارسی تضمین می‌کند هر دو خروجی ۴۱ عنوان، ۳ جدول، ۲۶ نمودار و صفر عنصر رابط کاربری داشته باشند.
- نسخهٔ وب، صفحهٔ معرفی، بستهٔ cPanel و خروجی Windows با اطلاعات انتشار 1.5.0 تازه می‌شوند.

## 1.4.1 — ۱۲ مرداد ۱۴۰۵

- هدر و وضعیت رابط در موبایل از دسکتاپ جدا شد تا باز و بسته‌کردن منو، ویرایشگر و کتابخانه مسیرهای یکدیگر را مسدود نکنند.
- دکمهٔ Back اکنون لایه‌های باز را به‌ترتیب می‌بندد و کاربر از منوها، کتابخانه و پنجره‌های ویرایش به مسیر قبلی برمی‌گردد.
- انتخاب‌گر فایل از منوی موبایل روی مرورگرهایی که محتوای پشت دیالوگ را غیرفعال می‌کنند نیز به‌درستی باز می‌شود.
- چیدمان موبایل از لبه‌ها بیرون نمی‌زند، هدف‌های لمسی اصلی حداقل ۴۴×۴۴ پیکسل هستند و متن‌های عملیاتی ریز خواناتر شده‌اند.
- نام‌گذاری و ظاهر دکمهٔ بستن کتابخانه روشن‌تر شد و تست‌های بازگشت، چیدمان و میان‌برها برای جلوگیری از بازگشت خطاها گسترش یافتند.
- بارگذاری بخش‌های سنگین و رندر Mermaid زمان‌بندی و سبک‌تر شد تا شروع برنامه و کار با سندهای بزرگ روان‌تر بماند.
- خروج از نمای تمام‌صفحهٔ Mermaid جای خواندن را حفظ می‌کند و دیگر سند را به ابتدای صفحه برنمی‌گرداند.
- نسخهٔ وب، صفحهٔ معرفی، بستهٔ cPanel و خروجی Windows با اطلاعات انتشار 1.4.1 تازه می‌شوند.

## 1.3.1 — ۱۲ مرداد ۱۴۰۵

- حالت مطالعه برای سندهای سنگین Mermaid پایدار شد و هنگام آماده‌شدن نمودارها صفحه دیگر جابه‌جا نمی‌شود.
- نمودارهای دور از دید به‌صورت تنبل رندر می‌شوند تا بازکردن و پیمایش سندهای بزرگ روان‌تر بماند.
- مهلت رندر هر نمودار از زمان واقعی اجرای آن محاسبه می‌شود تا انتظار در صف به خطای نادرست منجر نشود.
- نسخهٔ وب، صفحهٔ معرفی، بستهٔ cPanel و خروجی Windows با اطلاعات هات‌فیکس 1.3.1 تازه می‌شوند.

## 1.3.0 — ۱۰ مرداد ۱۴۰۵

- حالت مطالعه اکنون جای خواندن را با لنگر معنایی دقیق‌تر نگه می‌دارد و پس از reload، بازکردن فایل یا آماده‌شدن تصویر و Mermaid به همان بخش برمی‌گردد.
- استودیوی Mermaid با سازندهٔ سادهٔ فارسی، نمونه‌های بیشتر، سازگارسازی برچسب‌های فارسی و خطاهای روشن‌تر به‌روزرسانی شد.
- رندر نمودارها پایدارتر شد و از ساخت دوباره یا تکثیر ناخواستهٔ SVG هنگام اسکرول و نمایش تمام‌صفحه جلوگیری می‌شود.
- ذخیرهٔ وضعیت خواندن در نسخه‌های دسکتاپ Electron و Tauri مطمئن‌تر شد تا بازکردن مستقیم فایل از سیستم‌عامل سند قبلی را نشان ندهد.
- صفحهٔ معرفی، نسخهٔ وب، بستهٔ cPanel و نصب‌کنندهٔ Windows با اطلاعات انتشار 1.3.0 تازه می‌شوند.

## 1.1.0 — ۹ مرداد ۱۴۰۵

- ویرایشگر Markdown با فونت کدنویسی خواناتر، انتخاب متن پایدارتر و تجربهٔ بهتر برای نوشته‌های طولانی به‌روزرسانی شد.
- میان‌برهای صفحه‌کلید در ویرایشگر، کتابخانه، ذخیره و درج تصویر یکدست‌تر و قابل‌اعتمادتر شدند.
- نسخهٔ وب برای انتشار ایستا و بارگذاری روی هاست آماده‌تر شد و بستهٔ cPanel تازه تولید می‌شود.
- نسخهٔ دسکتاپ Windows با مسیر Tauri کم‌حجم‌تر، نصب‌کننده و فایل قابل‌حمل تازه منتشر می‌شود.
- صفحهٔ معرفی و لینک‌های دانلود با اطلاعات نسخهٔ تازه به‌روزرسانی می‌شوند.

## 1.0.0 — ۹ مرداد ۱۴۰۵

- نخستین نسخهٔ پایدار دسکتاپ برای Windows و بستهٔ قابل‌حمل Linux منتشر شد.
- صفحهٔ معرفی راوی اکنون Markdown و کاربردش را برای کاربران تازه‌کار توضیح می‌دهد.
- دانلودها، اندازهٔ فایل و راه بررسی اصالت هر نسخه ساده‌تر و روشن‌تر شده‌اند.
- دفتر تغییرات، حمایت از پروژه و کانال به‌روزرسانی‌ها مستقیماً در دسترس‌اند.
- مشکل افتادن نوشته‌های پیش‌نمایش زنده روی بخش بعدی برطرف شد.

## 0.20.1 — ۹ مرداد ۱۴۰۵

- تصویرهای اینترنتی با همان نشانی اصلی در سند می‌مانند تا فایل سبک‌تر باشد.
- تصویر فقط هنگام نیاز بارگذاری می‌شود و وابستگی آن به اینترنت پیش از درج توضیح داده می‌شود.

## 0.20.0 — ۹ مرداد ۱۴۰۵

- افزودن تصویر از دستگاه یا نشانی اینترنتی با یک پنجرهٔ ساده و میان‌بر `Alt+I` ممکن شد.
- تصویرهای محلی می‌توانند همراه سند `.ravi` جابه‌جا شوند.

## 0.19.1 — ۹ مرداد ۱۴۰۵

- ابزارهای نمای تمام‌صفحهٔ Mermaid در نوار ابزار پایین بوم یکجا شدند.

## 0.19.0 — ۹ مرداد ۱۴۰۵

- بزرگ‌نمایی با چرخ ماوس، جابه‌جایی نمودار و جا‌دادن کامل آن در کادر اضافه شد.

## 0.18.1 — ۹ مرداد ۱۴۰۵

- برچسب‌های فارسی نمودار حفظ می‌شوند و نمودار در حالت مطالعه تمام‌صفحه باز می‌شود.

## 0.18.0 — ۹ مرداد ۱۴۰۵

- ساخت و ویرایش نمودار Mermaid با پیش‌نمایش زنده و نمونه‌های آماده اضافه شد.
- راوی پیش از جایگزینی تغییرهای هم‌زمان به شما هشدار می‌دهد.

## 0.17.0 — ۹ مرداد ۱۴۰۵

- ساخت سند تازه با انتخاب `.md` یا `.ravi` ساده‌تر شد.
- پیش از کنارگذاشتن نوشتهٔ ذخیره‌نشده هشدار نمایش داده می‌شود.

## 0.16.0 — ۹ مرداد ۱۴۰۵

- شناسنامهٔ راوی، شمارهٔ نسخه و دفتر تغییرات از طریق نشان راوی در دسترس قرار گرفت.

## 0.15.0 — ۹ مرداد ۱۴۰۵

- اندازهٔ بخش‌های ویرایش و پیش‌نمایش قابل‌تنظیم شد و هر بخش را می‌توان جمع یا بازیابی کرد.

## 0.14.0 — ۸ مرداد ۱۴۰۵

- پس از انتخاب متن در ویرایشگر، ابزارهای پرکاربرد قالب‌بندی همان‌جا ظاهر می‌شوند.

## 0.13.0 — ۸ مرداد ۱۴۰۵

- پس از انتخاب متن در پیش‌نمایش، برجسته‌سازی، یادداشت و حاشیه‌نویسی در دسترس است.

## 0.12.0 — ۸ مرداد ۱۴۰۵

- پوستهٔ روشن و تاریک، میان‌برهای سازگار با صفحه‌کلید فارسی و سنجاق‌کردن فایل‌ها اضافه شد.

## 0.11.0 — ۸ مرداد ۱۴۰۵

- نوار بالای حالت مطالعه هنگام خواندن کنار می‌رود و با حرکت رو به بالا دوباره ظاهر می‌شود.

## 0.10.0 — ۸ مرداد ۱۴۰۵

- جهت نوشته‌های فارسی، انگلیسی و بخش‌های ترکیبی به‌صورت خودکار درست نمایش داده می‌شود.

## 0.9.0 — ۸ مرداد ۱۴۰۵

- حالت مطالعه با فهرست فصل‌ها و نوار کناری جمع‌شونده اضافه شد.

## 0.8.0 — ۸ مرداد ۱۴۰۵

- حرکت متن و پیش‌نمایش با هم هماهنگ شد تا جای فعلی نوشته گم نشود.

## 0.7.0 — ۸ مرداد ۱۴۰۵

- کتابخانه و تاریخچه در برگه‌های جدا قرار گرفتند و افزودن پوشه ساده‌تر شد.

## 0.6.0 — ۸ مرداد ۱۴۰۵

- نوار بالایی خلوت‌تر شد تا تمرکز روی سند بماند.

## 0.5.0 — ۸ مرداد ۱۴۰۵

- فهرست فرمان‌های صفحه‌کلید یکپارچه شد و با چیدمان فارسی و انگلیسی کار می‌کند.

## 0.4.0 — ۸ مرداد ۱۴۰۵

- هنگام ذخیره، نسخه‌های پیشین سند نگه‌داری می‌شوند و دسترسی به فایل‌های اخیر آسان‌تر است.

## 0.3.3 — ۸ مرداد ۱۴۰۵

- با قرارگرفتن نشانگر یا کلیک روی یادداشت، جزئیات آن به‌سادگی دیده می‌شود.

## 0.3.2 — ۸ مرداد ۱۴۰۵

- برجسته‌سازی، یادداشت و حاشیه‌نویسی همراه فایل `.ravi` قابل‌اشتراک شد.

## 0.3.1 — ۸ مرداد ۱۴۰۵

- راهنمای جابه‌جایی و اشتراک فایل `.ravi` به برنامه اضافه شد.

## 0.3.0 — ۷ مرداد ۱۴۰۵

- نسخهٔ دسکتاپ Windows منتشر شد و فایل‌های Markdown و Ravi مستقیماً از File Explorer باز می‌شوند.

## 0.2.0 — ۷ مرداد ۱۴۰۵

- پوشه‌های دستگاه به کتابخانه افزوده می‌شوند تا فایل‌های Markdown یکجا دیده شوند.

## 0.1.0 — ۷ مرداد ۱۴۰۵

- نخستین نسخهٔ راوی با ویرایش، پیش‌نمایش زنده و نمایش درست فارسی منتشر شد.
