# Product

<!-- impeccable:product-schema 1 -->

## Platform

وب و برنامه‌ی دسکتاپ ویندوز

## Users

کاربران فارسی‌زبانی که می‌خواهند یک فایل Markdown را بدون درگیری با ابزارهای فنی باز کنند، بخوانند، اصلاح کنند و خروجی بگیرند.

## Product Purpose

یک میز مطالعه و ویرایش سبک برای Markdown فارسی که فایل را با چینش راست‌به‌چپ درست نمایش می‌دهد و ویرایش و پیش‌نمایش را در یک تجربه‌ی روان کنار هم قرار می‌دهد. موفقیت یعنی کاربر بتواند فایل را با کشیدن یا انتخاب‌کردن باز کند، همان‌جا ویرایش کند، در حالت مطالعه بخواند و نسخه‌ی اصلاح‌شده را دانلود کند.

## Positioning

رفتار دو‌جهته‌ی دقیق: فایل کاملاً انگلیسی با چینش LTR نمایش داده می‌شود؛ در سند ترکیبی، هر بلوک با بیش از ۷۰٪ حروف لاتین LTR و سایر بلوک‌ها RTL است. قطعه‌کد و نشانی‌ها نیز جهت طبیعی خود را حفظ می‌کنند.

## Operating Context

استفاده در مرورگر دسکتاپ یا موبایل برای مرور یادداشت‌ها، مستندات و فایل‌های Markdown فارسی؛ با پشتیبانی از انتخاب فایل، کشیدن و رهاکردن، ویرایش زنده، پیش‌نمایش، حالت مطالعه و دانلود.

## پایداری و حمایت

راوی یک محصول مستقل است و تا همیشه رایگان می‌ماند. همهٔ بروزرسانی‌ها نیز از
طریق کانال تلگرام رسمی به‌صورت رایگان منتشر می‌شوند. حمایت مالی داوطلبانه فقط
برای پوشش هزینه‌های نگه‌داری، توسعه، ساخت نسخه‌های ویندوز و زیرساخت پروژه است.

- حمایت مالی: `https://daramet.com/poorsmile`
- کانال بروزرسانی‌ها: `https://t.me/poorsmile_crafts`
- وب‌سایت رسمی: `https://ravi.poorsmile.ir`
- نام حامی تنها با رضایت او و بر اساس نام نوشته‌شده در پیام حمایت، در
  «دربارهٔ راوی» نمایش داده می‌شود.

## Capabilities and Constraints

- ساخت فایل جدید با نام‌گذاری معتبر برای ویندوز، پسوند کنترل‌شده، انتخاب Markdown یا `.ravi`، عنوان اولیهٔ اختیاری، محافظت از تغییرات ذخیره‌نشده و انتخاب محل ذخیره
- ورودی فایل‌های `.md` و `.markdown` از طریق انتخاب فایل یا کشیدن و رهاکردن
- ویرایش زنده و پیش‌نمایش هم‌زمان
- تقسیم قابل‌تنظیم ویرایشگر و پیش‌نمایش، پنهان‌کردن مستقل هر پنل و جمع‌شدن پنل کوچک‌تر در آستانهٔ ۱۰٪
- نگه‌داری نسبت و حالت پنل‌ها روی دستگاه و کنترل دسترس‌پذیر تقسیم‌کننده با ماوس، لمس و صفحه‌کلید
- منوی شناور پنج‌آیکنی در محل انتخاب ادیتور برای پررنگ، مورب، کد، نقل‌قول و پیوند
- شناسنامهٔ محصول از طریق لوگوی هدر با نمایش نسخهٔ جاری، ویژگی‌های متمایز و تغییرات نسخه‌های اخیر
- تم روشن و تاریک سراسری با انتخاب ماندگار، تغییر نرم و میان‌بر مستقل از چیدمان صفحه‌کلید
- قفل اسکرول دوطرفه میان ادیتور و پیش‌نمایش بر اساس موقعیت نسبی محتوا
- حالت مطالعه‌ی تمام‌عرض
- فهرست خودکار فصل‌ها در سایدبار سمت راست حالت مطالعه با امکان جمع‌شدن و مشخص‌کردن بخش فعال
- هدر کم‌مزاحمت حالت مطالعه با نام فایل؛ پنهان هنگام خواندن رو به پایین و آشکار هنگام اسکرول رو به بالا
- دانلود محتوای ویرایش‌شده به‌صورت Markdown
- ذخیره‌ی محلی آخرین نوشته و تنظیمات در همان دستگاه
- اتصال پوشه با اجازه‌ی صریح کاربر، اسکن بازگشتی زیرپوشه‌ها و ساخت کتابخانه‌ی محلی فایل‌های Markdown
- جست‌وجو و مرور درختی کتابخانه بدون ارسال نام، مسیر یا محتوای فایل‌ها به سرور
- سنجاق‌کردن فایل‌های مهم در بخش مستقل بالای کتابخانه، با حفظ نمایش مشترک در پوشهٔ اصلی و نگه‌داری محلی وضعیت
- منوی شناور فشرده در محل انتخاب متن برای ساخت هایلایت، کامنت و حاشیه‌نویسی
- نمایش تمام‌صفحهٔ نمودارهای Mermaid در حالت مطالعه با زوم نقطه‌محور، جابه‌جایی با درگ و بازگشت امن به همان بخش متن
- نمایش تنبل تصویرهای اینترنتی با URL مستقیم و بدون ارسال referrer؛ فقط نشانی در Markdown و `.ravi` ذخیره می‌شود
- درج تصویر PNG، JPEG، WebP و GIF از فایل محلی با نگه‌داری محمولهٔ داخلی در فایل `.ravi`
- فونت ایران‌سنس به‌عنوان تعهد تایپوگرافی؛ در نبود فایل مجاز محلی، استفاده از پشته‌ی جایگزین فارسی
- بدون حساب کاربری، سرور ذخیره‌سازی یا ارسال محتوای فایل به سرویس خارجی
- نسخه‌ی دسکتاپ ویندوز با انتخاب‌گر بومی پوشه، پنجره‌ی ذخیره و نصب‌کننده‌ی آفلاین
- ثبت `.md` و `.markdown` در ویندوز و بازکردن مستقیم فایل از Explorer، با احترام به انتخاب برنامه‌ی پیش‌فرض کاربر

## Brand Commitments

نام محصول «راوی» است. رابط فارسی، آرام و دقیق است و فونت ایران‌سنس مبنای تایپوگرافی آن محسوب می‌شود.
هر دو تم باید سلسله‌مراتب «کاغذ، مرکب و آبی نمونه‌خوان» را حفظ کنند؛ تم تاریک نسخهٔ معکوس‌شدهٔ تم روشن نیست و سطوح و رنگ‌های وضعیت ترکیب مستقل دارند.

## Evidence on Hand

محتوای تجاری، مشتری، آمار عملکرد یا دارایی برند موجود نیست و نباید چنین ادعاهایی ساخته شود.

## Product Principles

- متن کاربر همیشه مهم‌تر از رابط است.
- تجربه‌ی فارسی باید از ابتدا RTL باشد، نه نسخه‌ی برعکس‌شده‌ی یک رابط LTR.
- ورود و خروج فایل باید ساده، محلی و قابل اعتماد باشد.
- کنترل‌های ضروری در دسترس‌اند و جزئیات اضافی مزاحم مطالعه نمی‌شوند.
- رفتار محتوای فنی و دو‌جهته باید قابل پیش‌بینی بماند.

## Accessibility & Inclusion

رابط باید با صفحه‌کلید قابل استفاده باشد، کنتراست خوانا داشته باشد، حرکت‌های غیرضروری را در حالت کاهش حرکت حذف کند و در نمایشگرهای کوچک بدون اسکرول افقی باقی بماند.
# Mermaid در راوی

راوی نمودار را جزئی از متن می‌داند، نه یک تصویر جانبی. منبع هر نمودار در یک
بلوک استاندارد `mermaid` نگه‌داری می‌شود و بدون تبدیل اختصاصی در `.md` و در
محتوای Markdown سند `.ravi` باقی می‌ماند. استودیوی نمودار باید ایجاد، بازبینی
و اصلاح همان بلوک را انجام دهد، scroll میز را حفظ کند و در تغییر هم‌زمان منبع
به‌جای بازنویسی خاموش، تعارض نشان دهد.

کنترل نمای نمودار در استودیو و تمام‌صفحهٔ مطالعه یک رفتار مشترک دارد: Wheel روی
نقطهٔ زیر ماوس زوم می‌کند، ابزار دست با درگ از خود نمودار یا فضای خالی Canvas
نما را جابه‌جا می‌کند و کنترل جا‌دادن با محاسبهٔ طول و عرض SVG، تمام نمودار را
در کادر قابل‌مشاهده قرار می‌دهد. در تمام‌صفحه، همهٔ این کنترل‌ها همراه با خروج
در یک Dock مرکزی پایین Canvas و با هدف کلیک حداقل ۴۴ پیکسلی قرار می‌گیرند.

رندر فقط در مرورگر، به‌صورت lazy، با Mermaid نسخهٔ پین‌شده، حالت امنیتی
`strict`، پاک‌سازی دوبارهٔ SVG، سقف اندازه و مهلت رندر انجام می‌شود. خروجی
نمودار نباید منبع خارجی، اسکریپت یا رویداد اجرایی فعال داشته باشد.

تصویرهای درج‌شده در Markdown با مرجع داخلی `raavi-image://` شناخته می‌شوند.
فایل `.ravi` دادهٔ تصویر، نام و نوع مجاز آن را کنار سند نگه می‌دارد؛ هنگام خروجی
Markdown همان مرجع به دادهٔ خودکفا تبدیل می‌شود تا فایل همراه، تصویر را از دست ندهد.
تصویرهای URL دادهٔ داخلی ندارند و فقط به‌صورت نشانی مستقیم باقی می‌مانند؛ بنابراین
فایل سبک است اما نمایش تصویر به اینترنت و ماندگاری منبع وابسته خواهد بود.
