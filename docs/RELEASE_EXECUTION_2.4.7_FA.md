# انتشار ۲.۴.۷ برای آزمون به‌روزرسان

مبنا: commit `16e6b6e` و payload نسخهٔ ۲.۴.۶. تغییرات فقط نسخهٔ package/lock و دفتر تغییرات برنامه است؛ قابلیت، وابستگی و قالب داده تغییر نکرده‌اند.

کاربر انتشار عمومی مانند ۲.۴.۶ را صریحاً درخواست کرد. اجازهٔ انتشار unsigned همچنان برقرار است. وب و لایت روی ۲.۴.۶ باقی می‌مانند.

بسته‌های Windows x64 و macOS arm64/x64 ساخته و پس از تطبیق کامل دریافت عمومی HTTPS منتشر شدند. manifest امضاشده در آخرین مرحله فعال شد؛ نسخهٔ قبلی و فایل‌های آن حفظ شده‌اند.

آزمون کاربر: شروع از ۲.۴.۶، دریافت ۲.۴.۷، اجرای نصاب، تأیید دسترسی مدیر در Windows، بازکردن برنامه و مشاهدهٔ ۲.۴.۷ و حفظ مخزن/اتصال‌ها. این آزمون هنوز روی دستگاه کاربر تأیید نشده است.

## تحویل نهایی — 2026-09-14T10:16:58.654977+00:00

- تگ `v2.4.7` روی commit `c981c2225a7582c11e1da0d605c6c473261b7c8b`؛ [Release عمومی](https://github.com/Poor-smile/RAVI/releases/tag/v2.4.7).
- [ساخت موفق هر دو معماری مک](https://github.com/Poor-smile/RAVI/actions/runs/34830811298) با مجوز unsigned؛ معماری باینری و صحت DMG بررسی شدند.
- ۳۵ آزمون اختصاصی updater، build و قرارداد نصاب Windows موفق‌اند. ۲۸۲۸ ورودی وابستگی بستهٔ قبلی حفظ شده‌اند؛ کد main/updater و کلید عمومی با نرمال‌سازی انتهای خط تغییر نکرده‌اند.
- همهٔ فایل‌های دانلود عمومی با اندازه، SHA-256 و SHA-512 تطبیق داده شدند. امضای Ed25519 سه معماری و امضای سازگاری Windows با کلید قبلی معتبرند.
- [سایت دانلود](https://ravi.poorsmile.ir/) و `updates/stable.json` روی ۲.۴.۷ فعال‌اند؛ وب و لایت ۲.۴.۶ باقی مانده‌اند.
- آزمون واقعی دریافت و نصب روی دستگاه کاربر هنوز در انتظار اجراست؛ ادعای موفقیت نصب واقعی یا گیت جامع تکرارشده نشده است.

| بسته | بایت | SHA-256 |
|---|---:|---|
| `Raavi-Setup-2.4.7-x64.exe` | 122431124 | `3cdf4498286bfb16d31363039cc57ba94c37a2467e1d5e4f167e9b54b6f87b36` |
| `Raavi-2.4.7-macOS-arm64-unsigned.dmg` | 156864728 | `0eced859e64c7a634ffaa46da9b589ef861c555c5882f39f564fdf9837e76fd9` |
| `Raavi-2.4.7-macOS-x64-unsigned.dmg` | 161780622 | `f860daa670eea0c2b1d5133cabec6999be3a761adebd268ec16ba2620a23a7f0` |

کد واقعی updater نسخهٔ ۲.۴.۶ با manifest عمومی روی Windows x64 و هر دو معماری مک اجرا شد و در هر سه مورد وضعیت `available` برای ۲.۴.۷ و آدرس بستهٔ متناظر را برگرداند. این بررسی شامل اجرای نصاب روی دستگاه کاربر نیست.
