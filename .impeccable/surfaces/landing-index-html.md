---
version: 1
slug: "landing-index-html"
primary_target: "landing/index.html"
related_targets: ["landing/style.css","landing/app.js","landing/landing.md"]
---

## Scope and mode

لندینگ ایستای آمادهٔ هاست وردپرسی برای معرفی و ترغیب کاربر فارسی‌زبان به تجربهٔ راوی.

## Audience, job, and action

کاربرانی که فایل‌های Markdown فارسی می‌سازند، مدیریت می‌کنند یا می‌خوانند. صفحه باید در چند ثانیه مسئله، تفاوت محصول و محلی‌بودن آن را روشن کند و سپس کاربر را به تجربهٔ نمونهٔ زنده، نسخهٔ وب، دانلود یا حمایت برساند.

## Proof, content, and constraints

تصاویر واقعی نرم‌افزار، یک فایل محتوای اصلی `landing.md`، ویرایش و پیش‌نمایش محلی، بازنشانی محتوا، بدون حساب کاربری، CDN، پایگاه داده یا build. وضعیت فایل‌های انتشار باید صادقانه باشد.

## Direction and memorable moment

استعارهٔ «برگهٔ زندهٔ نمونه‌خوانی» از هویت فعلی راوی: کاغذ سرد، جوهر ذغالی، آبی علامت‌گذاری و ستون متن/نتیجه. لحظهٔ ماندگار، تغییر مستقیم Markdown و دیدن نتیجه در همان سند معرفی است.

## Release inputs

نسخهٔ Windows 2.2.0 از هاست دانلود نسخه‌بندی‌شده و GitHub Release ارائه می‌شود.
خروجی‌های بومی macOS 2.2.0 برای Apple Silicon و Intel از GitHub Release رسمی
دریافت می‌شوند و چون فعلاً فاقد Developer ID و notarization اپل‌اند، وضعیت
`unsigned` باید کنار دکمه‌های دانلود، در جزئیات فایل و در متن انتشار شفاف بماند.
آپدیتر مشترک Windows و macOS بسته را بر اساس سیستم‌عامل و معماری انتخاب و با
SHA-512 و امضای Ed25519 اعتبارسنجی می‌کند. چون دارایی‌های macOS نسخهٔ 2.2.0 پیش
از این زیرساخت ساخته شده‌اند، صفحه باید فعال‌شدن آپدیتر macOS از نخستین build
بعدی و باقی‌ماندن GitHub Release به‌عنوان مسیر دستی جایگزین را شفاف بنویسد.
