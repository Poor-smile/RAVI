---
name: راوی
description: میز نمونه‌خوانی و ویرایش Markdown فارسی
colors:
  ink-950: "#171b18"
  ink-900: "#20251f"
  ink-700: "#50584f"
  ink-500: "#687168"
  paper-50: "#fcfdf9"
  paper-100: "#f5f6f0"
  paper-200: "#e8ebe2"
  paper-300: "#d5d9cf"
  proof-blue: "#2557e5"
  proof-blue-dark: "#1742bd"
  proof-blue-soft: "#e9efff"
  rule: "#cbd0c6"
  error: "#b9382f"
  error-soft: "#fff0ed"
  success: "#176f3b"
  saving: "#b77b08"
  window-close: "#c42b1c"
  focus-ring: "#9fb5ff"
  dark-ink-950: "#f2f5f1"
  dark-ink-900: "#e1e7e1"
  dark-ink-700: "#bdc6bd"
  dark-ink-500: "#aeb8af"
  dark-paper-50: "#181e1a"
  dark-paper-100: "#141a16"
  dark-paper-200: "#0e130f"
  dark-paper-300: "#29312b"
  dark-proof-blue: "#86a8ff"
  dark-proof-blue-dark: "#adc3ff"
  dark-proof-blue-soft: "#202e50"
  dark-rule: "#354039"
typography:
  display:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "clamp(30px, 4vw, 50px)"
    fontWeight: 700
    lineHeight: 1.5
    letterSpacing: "-0.025em"
  body:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "18px"
    fontWeight: 400
    lineHeight: 2
  heading-2:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "32px"
    fontWeight: 700
    lineHeight: 1.55
  heading-3:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "24px"
    fontWeight: 700
    lineHeight: 1.6
  settings-title:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "20px"
    fontWeight: 700
    lineHeight: 1.5
  settings-mobile-heading:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "26px"
    fontWeight: 700
    lineHeight: 1.46
  editor:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "16px"
    fontWeight: 400
    lineHeight: 1.75
  label:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "12px"
    fontWeight: 700
    lineHeight: 1.5
  small-label:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "11px"
    fontWeight: 400
    lineHeight: 1.5
  decorative:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "10px"
    fontWeight: 400
    lineHeight: 1.5
  micro-label:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "9px"
    fontWeight: 700
    lineHeight: 1.5
  brand:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "14px"
    fontWeight: 700
    lineHeight: 1.2
  quote-mark:
    fontFamily: "IRANSansX, IRANSans, Vazirmatn, Tahoma, Arial, sans-serif"
    fontSize: "28px"
    fontWeight: 700
    lineHeight: 1
  mono:
    fontFamily: "Vazir Code, Cascadia Code, Consolas, monospace"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.75
  math:
    fontFamily: "Libertinus Math, Cambria Math, serif"
    fontSize: "18px"
    fontWeight: 400
    lineHeight: 1.5
rounded:
  shell: "14px"
  settings-section: "16px"
  settings-status: "15px"
  settings-navigation: "10px"
  settings-control: "8px"
  guide: "2px"
  tight: "3px"
  control: "4px"
  tab: "5px"
  action: "6px"
  pill: "999px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "14px"
  lg: "24px"
  xl: "36px"
components:
  button-primary:
    backgroundColor: "{colors.proof-blue}"
    textColor: "{colors.paper-50}"
    typography: "{typography.label}"
    rounded: "{rounded.action}"
    padding: "0 15px"
    height: "36px"
  button-primary-hover:
    backgroundColor: "{colors.proof-blue-dark}"
    textColor: "{colors.paper-50}"
  button-quiet:
    backgroundColor: "transparent"
    textColor: "{colors.paper-300}"
    typography: "{typography.label}"
    rounded: "{rounded.action}"
    padding: "0 15px"
    height: "42px"
  tool-button:
    backgroundColor: "transparent"
    textColor: "{colors.ink-700}"
    rounded: "{rounded.control}"
    size: "31px"
  pane:
    backgroundColor: "{colors.paper-50}"
    rounded: "0"
---

# Design System: راوی

## Overview

**Creative North Star: "برگه‌ی زنده‌ی نمونه‌خوانی"**

راوی از فضای کار یک نمونه‌خوان حرفه‌ای می‌آید: کاغذ روشن، مرکب دقیق، حاشیه‌های کاربردی و نشانه‌هایی که تنها وقتی لازم‌اند خودشان را نشان می‌دهند. نوشته همیشه بیشترین سطح و بالاترین کنتراست را دارد؛ رابط مثل ابزار چاپ و صحافی کنار آن می‌نشیند، نه روی آن.

هویت در تمام سطوح با قاب‌بندی دقیق، خط‌های ثبت، شماره‌ی برگ و وضعیت‌های شبیه مهر یا یادداشت نمونه‌خوان باقی می‌ماند. تزئین چاپی فقط وقتی مجاز است که جهت، وضعیت یا مرز یک کار را روشن کند.

**Key Characteristics:**

- روشن، دقیق و مناسب مطالعه‌ی طولانی
- ترکیب کاغذ سرد، مرکب زغالی و آبی نمونه‌خوان
- سطوح کاری تخت و یک سایه‌ی واقعی برای برگ حالت مطالعه
- حرکت کوتاه شبیه جابه‌جایی یا بازشدن یک برگه

## Colors

راهبرد رنگی محدود است: خنثی‌های چاپی با یک آبی واضح برای عمل و تمرکز. مرکب زغالی متن و نوار اصلی را می‌سازد، خانواده‌ی کاغذ فاصله‌ی سطوح را بدون سایه روشن می‌کند، و قرمز فقط به خطا تعلق دارد.

### Primary

- **آبی نمونه‌خوان:** عمل اصلی، فوکوس، لینک و نشانه‌ی ثبت
- **آبی نمونه‌خوان تیره:** حالت hover و لینک‌های متنی
- **آبی یادداشت:** پس‌زمینه‌ی کم‌کنتراست کنترل فعال

### Neutral

- **مرکب عمیق:** متن اصلی و نوار بالای محصول
- **مرکب نرم:** متن توضیحی و آمار سند
- **کاغذ سفید سرد:** سطح پیش‌نمایش
- **کاغذ کار:** ویرایشگر و نوارهای ابزار
- **خط صحافی:** مرزها و تقسیم‌کننده‌ها

**The Proofreader Blue Rule.** آبی فقط عمل اصلی، مکان فعال و نشانه‌ی نمونه‌خوان را حمل می‌کند؛ به رنگ تزئینی پس‌زمینه تبدیل نمی‌شود.

### Light and Dark Themes

تم روشن و تاریک دو ترکیب مستقل از یک زبان بصری‌اند. در تم تاریک، کاغذ به چهار سطح سبزـزغالی تبدیل می‌شود، متن اصلی روشن می‌ماند و آبی نمونه‌خوان برای حفظ کنتراست روشن‌تر می‌شود. نوارهای کروم، ورودی‌ها، هشدارها و رنگ‌های حاشیه‌نویسی توکن معنایی جدا دارند تا هیچ متن روشن روی سطح روشن یا متن تیره روی سطح تیره قرار نگیرد.

سوییچ تم کنار نام راوی قرار می‌گیرد. تغییر تم بی‌درنگ ثبت می‌شود و رنگ سطح‌ها در بازه‌ای کوتاه نرم جابه‌جا می‌شوند؛ تنها حرکت ویژه یک هالهٔ محدود پشت هدر است و هرگز روی متن یا تمام صفحه قرار نمی‌گیرد. در حالت `prefers-reduced-motion` تغییر بدون انیمیشن انجام می‌شود.

## Typography

**Display Font:** IRANSansX با fallback فارسی  
**Body Font:** IRANSansX با fallback فارسی  
**Label/Mono Font:** `Vazir Code` با fallbackهای Cascadia Code و Consolas؛ فقط برای کد، مسیر، URL و دادهٔ فنی
**Math Font:** `Libertinus Math` با fallbackهای Cambria Math و serif؛ فقط برای glyph و پیش‌نمایش فرمول

**Character:** وزن‌ها و اندازه‌های ایران‌سنس سلسله‌مراتب را می‌سازند. عنوان اصلی بسیار سنگین و فشرده است، بدنه با فاصله‌ی خط دو برابر اندازه‌ی متن نفس می‌کشد. کد همیشه LTR و از متن فارسی قابل تشخیص است.

### Hierarchy

- **Display** (وزن ۷۰۰، `clamp(30px, 4vw, 50px)`، فاصله‌ی خط ۱٫۵): عنوان سطح اول نوشته
- **Live H2** (وزن ۷۰۰، ۳۲px، فاصله‌ی خط ۱٫۵۵): تیتر سطح دوم در ویرایش روان
- **Live H3** (وزن ۷۰۰، ۲۴px، فاصله‌ی خط ۱٫۶): تیتر سطح سوم در ویرایش روان
- **Headline** (وزن ۷۰۰، `clamp(23px, 2.5vw, 30px)`، فاصله‌ی خط ۱٫۵): بخش‌های اصلی
- **Title** (وزن ۷۰۰، `clamp(19px, 2vw, 23px)`): زیربخش‌ها
- **Body** (وزن ۴۰۰، ۱۸px، فاصله‌ی خط ۲): خواندن طولانی با عرض حداکثر ۷۶ch
- **Label** (وزن ۷۰۰، ۱۲px): کنترل‌ها و وضعیت
- **Interactive floor:** هیچ label یا متن داخل کنترل تعاملی در دسکتاپ کمتر از ۱۲px و در موبایل/ورودی لمسی کمتر از ۱۳px نیست.
- **Caption** (۱۱px): فقط متن تزئینی یا metadata غیرتعاملی؛ step ده‌پیکسلی فقط برای جزئیات decorative بدون نقش عملیاتی مجاز است.

**The Native Direction Rule.** فایل بدون حروف فارسی/عربی و دارای متن لاتین، یکپارچه LTR است. در سند ترکیبی، جهت هر بلوک از نسبت حروف خواندنی آن تعیین می‌شود: بیش از ۷۰٪ لاتین LTR و باقی RTL. خط زیر عنوان، نقل‌قول، فهرست و جدول باید از ویژگی‌های منطقی CSS پیروی کنند تا تزئینات نیز همراه متن جابه‌جا شوند.

### Iconography

راوی در تمام chrome، فرمان‌ها، پنل‌ها، dialogها و حالت‌های ویرایش از خانوادهٔ
`Google Material Symbols Rounded` استفاده می‌کند. هندسهٔ Rounded با گوشه‌های
کنترل‌های راوی هماهنگ است، اما آیکن نباید به تزئین یا جایگزین متن ضروری تبدیل
شود.

- وزن پایه `400` و شبکهٔ طراحی مرجع `48` است؛ خروجی SVG در اندازه‌های ۱۶، ۱۸،
  ۲۰ و ۲۴ پیکسل بدون stroke سفارشی مقیاس می‌شود.
- حالت عادی از مسیر Outline و حالت انتخاب‌شده، pinned یا pressed از مسیر Filled
  همان Symbol استفاده می‌کند؛ تغییر خانواده یا آیکن دوم برای بیان یک state مجاز
  نیست.
- رنگ همیشه `currentColor` و از توکن معنایی کنترل می‌آید. آبی نمونه‌خوان فقط
  active/focus/action را حمل می‌کند.
- آیکن تزئینی `aria-hidden` است؛ کنترل icon-only باید نام فارسی روشن، tooltip
  قابل focus و hit area مصوب را داشته باشد.
- source رسمی SVG با وزن ۴۰۰ در
  `app/icons/material-symbol-paths.ts` نگه‌داری و با
  `npm run icons:generate` از بستهٔ `@material-symbols/svg-400` بازتولید می‌شود.
  emoji، رسم دستی و ترکیب خانواده‌های Lucide/Material در رابط محصول ممنوع است.

## Layout

سطح اصلی یک میز تک‌سندی است: در «ویرایش روان» و «متن خام»، ادیتور تمام سطح میانی را می‌گیرد و ابزارها داخل header محلی یا نزدیک selection/خط فعال ظاهر می‌شوند. «نمونه‌خوانی دوبرگی» حالت ممتاز و اختیاری راوی است: ویرایشگر در سمت چپ فیزیکی، پیش‌نمایش در سمت راست فیزیکی و ستون ثبت میان آن‌ها قرار می‌گیرد. ستون ثبت قابل‌درگ است و نسبت آخر و حالت میز روی همان دستگاه می‌ماند. پنل‌های زمینه‌ای در دسکتاپ از لبهٔ فیزیکی راست باز می‌شوند؛ rail با عرض ثابت ۵۶px در تم روشن از `paper-100` و در تم تاریک از `chrome-900` استفاده می‌کند تا حافظهٔ مکانی و تفکیک سطح حفظ شود و pane کنار آن باز یا جمع می‌شود. در عرض‌های فشرده، همین ترکیب به drawer سمت راست تبدیل می‌شود.

chrome دسکتاپ دو تراز متراکم دارد: نوار عنوان ۳۶ پیکسلی و نوار فرمان ۵۶ پیکسلی. کنترل‌ها ۳۶ پیکسل‌اند؛ در موبایل target واقعی حداقل ۴۴ پیکسل باقی می‌ماند. باقی ارتفاع به نوشته تعلق دارد و برگ خواندن روی اندازه‌ی ۷۶ch محدود می‌شود.

### قرارداد chrome تک‌سندی

نوار عنوان در مرکز، هویت کوچک «راوی»، میان‌بر قرصی `CTRL+K` و کنترل گرد تم را دارد. کنترل‌های Minimize، Maximize/Restore و Close در سمت راست فیزیکی Windows قرار می‌گیرند و با RTL آینه نمی‌شوند. نوار فرمان زیر آن، فرمان‌های فایل و overflow را در چپ فیزیکی نگه می‌دارد و در حالت سند فعال هویت سند، mode، وضعیت ذخیره و مطالعه را طبق screen مربوط اضافه می‌کند. کتابخانه از rail راست کنترل می‌شود و دکمهٔ جدا در header ندارد. خروجی، حمایت، میان‌برها و دربارهٔ راوی در overflow عمومی قرار می‌گیرند؛ overflow در desktop یک popover متراکم و در موبایل یک sheet لمسی است. active، expanded و dirty علاوه بر رنگ با `aria-*`، تغییر متن، تغییر آیکن یا خط زیرین قابل تشخیص‌اند.

در Empty State فقط سه فرمان icon-only «بازکردن فایل»، «سند جدید» و «بیشتر» در نوار فرمان دیده می‌شوند. سطح میانی `editor-surface-bg` است و کارت بدون border با اندازهٔ مرجع ۵۸۸×۳۶۰px، شعاع ۱۴px و پس‌زمینهٔ `paper-200` دقیقاً وسط stage قرار می‌گیرد. آیکن ۶۴×۶۴px با glyph هجده‌پیکسلی روی `proof-blue-soft`، تیتر ۳۰/۴۵، متن ۱۸/۳۶ و دو عمل ۳۶px دارد. در تم تاریک، Title Bar و کارت `paper-200`، Command Bar برابر `paper-50`، stage برابر `editor-surface-bg` و Rail برابر `chrome-900` هستند؛ این سطوح نباید به یک رنگ تخت ادغام شوند. تا پیش از بازشدن سند فقط «کتابخانه» و «تاریخچه» در rail فعال‌اند؛ پنج مقصد وابسته به سند disabled با opacity مرجع ۴۲٪ و نام دسترس‌پذیر باقی می‌مانند.

در W03، «سند جدید» کاربر را مستقیماً به draft محلی «سند جدید» در حالت نوشتن می‌برد؛ حالت ذخیره «هشدار: ذخیره نشده» با مرز ۱px و شعاع ۴px است و هر هفت مقصد rail فعال می‌شوند. نام سند یک کنترل شفاف ۱۳۲×۳۶px است که منوی سند را با فرمان‌های ذخیره، ذخیره با نام و خروجی باز می‌کند. Tab Bar مصوب ۳۶px بالای Stage باقی می‌ماند؛ بنابراین سطح سند در قاب مرجع ۱۲۸۰×۹۱۴ دقیقاً ۷۶۰×۷۵۴px و در مختصات ۲۳۲×۱۴۴ قرار می‌گیرد. سطح سند `paper-50` و شعاع ۱۴px دارد و border یا shadow تزئینی نمی‌گیرد؛ در عرض کمتر، سند به‌اندازه‌ای جمع می‌شود که فضای ۸۲px موردنیاز Gutter در هر دو سوی مرکز محفوظ بماند. بلوک غیرفعال رندرشده و بدون مرز است؛ فقط بلوک فعال ۷۶۰×۶۲px با `proof-blue-soft` و مرز یک‌پیکسلی focus دیده می‌شود. Gutter یک sibling بیرون سطح سند و ۸px سمت راست آن است: ۷۴×۳۶px، کنترل نوع بلوک نزدیک سند و دستگیرهٔ drag در لبهٔ بیرونی؛ drag واقعی و جایگزین Alt+↑/↓ باید کل بلوک فعال را جابه‌جا کند. نوار وضعیت فشرده ۲۲۰×۲۰px، بدون border و icon، ۹px بالاتر از لبهٔ پایین سند قرار می‌گیرد و آمار واقعی «واژه · خط» را با Caption نشان می‌دهد.

در W04، ورود مکان‌نما یا selection به یک بلوک متنیِ پرشده همان Block Shell فعال ۶۲px را با تایپ `Editor/Live` برابر ۱۶/۳۴، `proof-blue-soft` و focus ring نشان می‌دهد؛ بقیهٔ بلوک‌ها در حالت Rendered و بدون مرز می‌مانند. در قاب مرجع، بلوک فعال در مختصات ۲۳۲×۳۵۲ و Gutter در ۱۰۰۰×۳۵۲ قرار می‌گیرند، بنابراین Gutter بلوک پرشده با لبهٔ بالای Shell هم‌ردیف است؛ فقط برای بلوک خالی W03 شانزده پیکسل بالاتر می‌ایستد. هنگام selection، نوار پایین بی‌درنگ به `proof-blue-soft`/`proof-blue` تغییر می‌کند و آمار کاملاً محلی «واژهٔ انتخاب‌شده · نویسه» را از همان بازهٔ واقعی نشان می‌دهد؛ با جمع‌شدن selection دوباره «واژه · خط» سند نمایش داده می‌شود. انتخاب بخشی از متن یک سلول جدول نیز دقیقاً همین Selection Toolbar نه‌عملی را در مجاورت همان سلول باز می‌کند؛ قالب‌بندی روی draft همان سلول اعمال می‌شود و selection تا اجرای فرمان یا بازگشت focus حفظ می‌ماند. `Escape` نوار قالب‌بندی selection را بدون ازبین‌بردن بازه می‌بندد و تا تغییر واقعی selection آن را دوباره باز نمی‌کند. در جدول، `ArrowUp` از ردیف عنوان و `ArrowDown` از آخرین ردیف ابتدا draft سلول را ثبت می‌کنند، سپس از جدول خارج می‌شوند و نزدیک‌ترین بلاک مستقل قبلی یا بعدی را ــ با ردکردن فاصله‌های خالی ــ فعال می‌کنند؛ بنابراین ورود کیبوردی به Quote چندخطی یا هر بلاک مجاور به ماوس وابسته نیست. وضعیت‌های خنثی ذخیره و Saving border ندارند؛ فقط Dirty و Error مرز معنایی خود را می‌گیرند.

جدول در زنجیرهٔ عمودی سند یک مقصد واقعی Focus است: `ArrowDown` از مرز بلاک بالایی وارد Header سمت راست (`column 0`) می‌شود و `ArrowUp` از مرز بلاک پایینی وارد پایین‌ترین سلول همان ستون می‌شود؛ سپس جهت‌ها سلول‌به‌سلول حرکت می‌کنند و فقط در مرز نخست/آخر از جدول خارج می‌شوند. سلول غیرفعال Markdown درون‌خطی مجاز مانند Bold، Italic، Underline، Strike، Inline Code، Link و Highlight را رندر می‌کند؛ سلول در حال تایپ منبع Markdown را نشان می‌دهد و اجرای Selection Toolbar با حفظ Focus ابتدا نتیجهٔ رندرشده را نمایش می‌دهد تا کاربر اثر فرمان را فوراً ببیند. راهنمای context-sensitive جدول همیشه بلافاصله بعد از Shell جدول در جریان سند باقی می‌ماند و هنگام ورود کیبوردی با کمترین Scroll لازم داخل viewport آشکار می‌شود.

زیر بلاک فعال در ویرایش روان، یک راهنمای context-sensitive در جریان سند قرار می‌گیرد؛ این ردیف با اندازهٔ Micro ۹px، بدون border و سایه، فقط میان‌برهای معتبر همان وضعیت را نشان می‌دهد. Empty مسیرهای `Ctrl/Cmd+Enter` و `/`، List رفتارهای Enter، Shift+Enter و Nest/Outdent، Table انتخاب سلول و پیمایش سلولی، Selection دسترسی `Alt+F10` و Block Selected فرمان‌های لغو، تکثیر و جابه‌جایی را نمایش می‌دهد. راهنما در Source/Code ظاهر نمی‌شود و از «تنظیمات ← ویرایش ← راهنمای کلیدهای بلاک فعال» به‌صورت محلی و پایدار خاموش یا روشن می‌شود؛ مقدار پیش‌فرض روشن است.

در W05، کلیک کنترل نوع بلوک یا واردکردن `/` در بلوک خالی، منوی ۳۲۰×۵۰۶px «نوع بلوک» را در امتداد چپ Gutter باز می‌کند؛ در قاب ۱۲۸۰×۹۱۴ مختصات آن دقیقاً ۶۸۰×۳۶۰ است. سطح منو `paper-50`، شعاع ۶px، بدون border خنثی و با سایهٔ popover است؛ عنوان فقط یک ردیف ۳۶px دارد. دوازده انتخاب ۳۶px با آیکن‌های Google Material Symbols Rounded در سه گروه و همین ترتیب‌اند: «تیتر ۱، تیتر ۲، تیتر ۳، متن معمولی»؛ «چک‌لیست، فهرست بولت، فهرست مراحل»؛ «کد، نقل‌قول، جدول، Mermaid، تصویر». دو جداکنندهٔ ۳۰۴×۱px گروه‌ها را جدا می‌کنند و Callout در این سطح حضور ندارد. گزینهٔ نوع فعلی و کنترل باز با `proof-blue-soft` و `proof-blue` مشخص می‌شوند. `↑/↓` میان گزینه‌ها می‌چرخد، `Home/End` ابتدا و انتها را می‌گیرد، `Enter` تأیید و `Escape` بستن همراه با بازگرداندن focus است. تبدیل مستقیم باید کل بازهٔ نحوی بلوک ــ از جمله fenced block چندخطی ــ را تبدیل کند و متن و موقعیت منطقی cursor را حفظ کند؛ جدول و تصویر helper محلی خود را باز می‌کنند و Mermaid مستقیماً Graph Studio را فراخوانی می‌کند. در هر سه مسیر، نویسهٔ trigger یعنی `/` پیش از درج یا بازشدن ابزار پاک می‌شود. اگر کاربر پس از `/` به تایپ query ادامه دهد، منوی بلوک جمع می‌شود و completion سراسری فرمان‌ها همچنان فعال می‌ماند.

در W06، همان هندسه و رفتار W03/W04 بدون شاخهٔ رفتاری تازه به تم تاریک نگاشت می‌شود: Title Bar برابر `paper-200`، Command Bar و سند برابر `paper-50`، stage برابر `editor-surface-bg` و rail برابر `chrome-900` است. سند بازشده با نام «راهنمای نگارش.md» در حالت نوشتن، وضعیت ذخیرهٔ خنثی و بدون border دارد. فقط Block Shell فعال از `proof-blue-soft` با focus ring روشن استفاده می‌کند؛ بلوک‌های غیرفعال همچنان Rendered و بی‌مرز می‌مانند و تغییر تم نباید active block، selection، scroll یا محتوای سند را عوض کند. کنترل حالت فعال روی `chrome-950` از foreground معنایی `chrome-fg` استفاده می‌کند تا در هر دو تم خوانا بماند. Gutter، نوار وضعیت و سطح سند دقیقاً مختصات مرجع W03 را حفظ می‌کنند و رنگ آن‌ها فقط از توکن‌های Dark می‌آید.

در W07، «نمونه‌خوانی دوبرگی» داخل stage ۱۲۲۴×۸۲۲ یک Split Workspace با inset شانزده‌پیکسلی می‌سازد. دو برگ مستقل ۵۶۰×۷۵۰px با `paper-50`، شعاع ۱۴px و بدون border یا shadow در مختصات مرجع ۲۸×۱۲۸ و ۶۳۶×۱۲۸ قرار می‌گیرند: ویرایش منبع در چپ فیزیکی و پیش‌نمایش رندرشده در راست. Pane Header دقیقاً ۵۶px است؛ کنترل جمع‌کردن ۳۶px در چپ فیزیکی، cluster عنوان در راست و active mark سه‌پیکسلی با inset بیست‌پیکسلی دارد. folio و icon well فقط در Pane فعال `proof-blue-soft` می‌شوند و focus/pointer/wheel فعال‌بودن Pane را جابه‌جا می‌کند. Divider در میانه hit area شانزده‌پیکسلی و rule یک‌پیکسلی دارد؛ Arrowها آن را پنج درصد در جهت فیزیکی حرکت می‌دهند، Enter نسبت ۵۰/۵۰ را برمی‌گرداند و Home/End Pane متناظر را جمع می‌کنند. جمع‌شدن یک Pane فقط ترکیب را تک‌برگی می‌کند و session، chrome، شعاع‌ها، Header و Registration Spine همین W07 را تا بازگردانی Pane حفظ می‌کند. Registration Spine برابر ۳۲×۳۶۰px با `paper-100`، دو registration dot، کنترل link و برچسب محلی «اسکرول هماهنگ» است؛ drag threshold، sync و بازگرداندن Pane از همان state محلی موجود استفاده می‌کنند. متن منبع با `Editor/Source` برابر ۱۵/۳۲ و پیش‌نمایش با H2 برابر ۳۰/۴۵ و Body برابر ۱۸/۳۶ رندر می‌شود.

در W08، حالت Code همان نمونهٔ واقعی CodeMirror و همان source سند را در تمام سطح آزاد Stage نشان می‌دهد؛ editor دوم، کارت سند، عنوان تزئینی یا متن نمایشیِ جعلی ساخته نمی‌شود. Tab Bar مصوب ۳۶px را نگه می‌دارد و سطح کد در Stage مرجع ۱۲۲۴×۷۸۶ از هر چهار لبه پر می‌شود؛ `paper-50`، بدون border، shadow یا radius. شمارهٔ خط و fold gutter برای راهبری فایل دیده می‌شوند، اما Gutter بلوک، Block Shell، انتخاب ساختاری و Render بلوک غنی وجود ندارند. source با inset برابر ۳۲×۲۴px، فونت Cascadia Code با fallback محلی Vazir Code و اندازهٔ ۱۳/۲۳ ویرایش می‌شود. جهت هوشمند هر خط و isolation قطعه‌های کد حفظ می‌شود؛ focus، selection، history، undo/redo، جست‌وجو و ذخیره همچنان روی همان state و transactionهای موجود کار می‌کنند. `Ctrl/Cmd+A` کل فایل را انتخاب می‌کند، Enter خط متنی می‌سازد، Tab/Shift+Tab تورفتگی کد را تغییر می‌دهند و `/` نویسهٔ واقعی Markdown است؛ Slash Menu و انتخاب دیداری بلوک در این نما وجود ندارند، اما قرارداد سراسری `Ctrl/Cmd+Enter` همچنان کل بلوک نحوی زیر نشانگر را تأیید و یک Text Block مستقل بعد از آن ایجاد می‌کند. Chrome همان ترتیب فیزیکی مرجع را دارد، segment Code انتخاب است و rail هفت‌گانه در حالت بسته باقی می‌ماند.

در W09، Overflow دسکتاپ یک `menu` واقعی و متراکم در مختصات ۱۶×۹۶ قاب مرجع است: viewport سطح ۳۲۰×۲۹۴px با `paper-50`، مرز یک‌پیکسلی `rule`، شعاع ۶px و بدون scrim یا shadow. padding دیداری چهارپیکسلی، عنوان تک‌ردیف ۳۶px و محتوای قابل‌پیمایش شامل هفت ردیف ۳۱۲×۳۶px دارد: «فرمان‌های راوی»، «میان‌برهای صفحه‌کلید»، «تنظیمات»، «خروجی Word یا PDF»، عمل پویای «تم روشن/تم تاریک»، «حمایت از راوی» و «دربارهٔ راوی». دو separator برابر ۳۰۴×۱px پس از «تنظیمات» و عمل تم می‌آیند. آیکن‌ها فقط Google Material Symbols هستند؛ میان‌برهای واقعی `Ctrl+K`، `Ctrl+/`، `Ctrl+Shift+E` و `Alt+T` با Cascadia Code برابر ۱۳/۲۳ در انتهای فیزیکی چپ نشان داده می‌شوند. `↑/↓` انتخاب را حلقوی جابه‌جا می‌کند، `Home/End` ابتدا و انتها، `Enter` اجرای همان command و `Escape` بستن همراه با بازگرداندن focus به دکمهٔ «بیشتر» است. `Tab/Shift+Tab` برخلاف Sheet موبایل trap نمی‌شوند: Popover بسته و focus در ترتیب طبیعی سند ادامه پیدا می‌کند. هر Dialog بازشده از menu به همان دکمهٔ «بیشتر» بازمی‌گردد، اما همان فرمان اگر از editor، chrome یا Command Palette فراخوانی شود opener واقعی همان invocation را نگه می‌دارد. با انتقال focus از سند به این menu، Block Shell و Gutter ویرایشی دیده نمی‌شوند و متن در حالت Rendered می‌ماند؛ آمار پایین همچنان از دادهٔ واقعی محلی سند می‌آید. نسخهٔ موبایل همین فرمان‌ها را در Sheet لمسی موجود با target حداقل ۴۴px ارائه می‌کند.

در W10، شکست ذخیره هم‌زمان در دو سطح و فقط با رنگ معنایی Error اعلام می‌شود: Save Status برابر ۱۳۲×۳۶px متن «ذخیره ناموفق»، نقطهٔ هفت‌پیکسلی و مرز یک‌پیکسلی `error` دارد؛ Banner ماندگار ۵۸۸×۶۸px بدون border یا shadow در مرکز لبهٔ بالای سطح سند و مختصات ۳۱۸×۱۴۴ قاب مرجع قرار می‌گیرد. Banner از `error-soft` و شعاع ۱۴px استفاده می‌کند و به‌ترتیب RTL شامل Material Symbol «warning»، عنوان ۱۲/۱۸ «هماهنگ‌سازی فایل انجام نشد»، توضیح ۱۱/۱۷ «فایل در دسترس نیست یا مجوز آن تغییر کرده است. مسیر را بررسی کنید.»، عمل ۳۶px «تلاش دوباره» و کنترل ۳۶px بستن با Material Symbol «close» است. retry دقیقاً همان عملیات شکست‌خورده و context آن را دوباره اجرا می‌کند؛ برای Save As نام و نوع فایلِ همان تلاش حفظ می‌شوند و به Save Current تبدیل نمی‌شود. شکست Save As، Dialog را همان لحظه می‌بندد تا workspace دیگر inert نباشد و Banner فوراً دیده، اعلام و قابل‌عمل باشد. هنگام retry، Banner جمع و Save Status به Saving بدون border می‌رود؛ موفقیت Saved و شکست دوباره همان W10 را برمی‌گرداند. بستن فقط پیام سطح سند را جمع می‌کند و وضعیت Error را پنهان یا به Saved تبدیل نمی‌کند؛ focus در هر دو مسیر به Save Status بازمی‌گردد. حضور Banner محتوای W10 را چهل پیکسل پایین‌تر می‌برد؛ Block Shell، Gutter و placeholder بلوک خالی دیده نمی‌شوند، اما عرض ۷۶۰px سند، rail، نوار آمار و دادهٔ محلی سند تغییر نمی‌کنند. پیام خطای عمومی برنامه جایگزین این Banner نیست و خطاهای غیرذخیره همچنان مسیر مستقل خود را دارند؛ در ورودی لمسی targetهای تعاملی Banner حداقل ۴۴px می‌شوند. فقط در عرض حداکثر ۸۲۰px متن کامل عنوان و توضیح بدون ellipsis در چیدمان چندردیفی می‌آید؛ coarse pointer دسکتاپ، هندسه و layout دسکتاپ را حفظ می‌کند.

در R01، نمای «خواندن / بسته» در قاب مرجع ۱۱۸۰×۸۵۸ از Title Bar برابر ۳۶px، Reading Header برابر ۱۱۲۴×۶۴px و rail روشن ۵۶px در راست فیزیکی تشکیل می‌شود. Title Bar فقط نام مرکزی «راوی» و نشان محلی «● فقط روی این دستگاه» را نگه می‌دارد. Header با `paper-50` و شعاع ۱۴px، هویت `راهنمای نگارش.md` را در راست و دو عمل «بازگشت به میز» و ellipsis ابزار مطالعه را در چپ فیزیکی دارد؛ جست‌وجو، فهرست، هایلایت و نظر در Header تکرار نمی‌شوند. rail اختصاصی Reading دقیقاً چهار Material Symbol «جست‌وجو در متن»، «فهرست سند»، «هایلایت‌ها» و «نظرات» را در کنترل‌های ۳۶×۳۶px بی‌مرز نگه می‌دارد و در حالت Closed هیچ‌کدام active نیستند. در دسکتاپ حتی در عرض ۱۱۸۰px rail در جریان layout باقی می‌ماند و pane پس از فراخوانی در کنار آن dock می‌شود؛ فقط زیر ۸۲۱px به Drawer modal تبدیل می‌شود.

برگ Reading در مختصات ۱۸۲×۱۲۰ قاب مرجع، دقیقاً ۷۶۰×۶۸۰px، `paper-50`، شعاع ۱۴px، بدون border و با تنها سایهٔ مجاز مطالعه `0 18px 54px shadow-color` است. inset داخلی ۴۴px بالا، ۴۸px افقی و ۱۶px پایین است؛ Kicker با وزن ۴۰۰ برابر ۱۱/۱۷، H1 بدون tracking برابر ۵۰/۷۵ و Body برابر ۱۸/۳۶ رندر می‌شوند. نقل‌قول روی `paper-100` با شعاع ۶px و بدون border می‌آید. پیش‌نمایش Mermaid برابر ۶۶۴×۱۷۲px با شعاع ۱۴px، metadata «نمودار فرایند / Mermaid · رندر شده در سند»، سطح رندر داخلی و عمل ۱۴۲×۳۶px «تمام‌صفحه» است؛ همان SVG و anchor سند به Graph Viewer منتقل می‌شوند و نسخهٔ دوم ساخته نمی‌شود. نوار آمار واقعی و محلی «واژه · خط» با اندازهٔ ۳۳۶×۲۸px و بدون icon یا border، شانزده پیکسل بالاتر از لبهٔ برگ می‌نشیند. ورود و خروج Reading باید anchor معنایی، selection، focus و scroll سند را بدون transaction محتوایی حفظ کند.

در R02، هندسه و رفتار R01 بدون تغییر و با mode تاریک Foundation بازتولید می‌شود؛ این حالت پوستهٔ تیرهٔ جداگانه یا فیلتر رنگ نیست. Title Bar و نوار آمار `paper-100`، Workspace و پوسته `paper-200`، Header و برگ سند `paper-50` و Reading Rail به‌طور مشخص `chrome-900` هستند. نام مرکزی «راوی» و آیکن‌های Rail از `ink-700` و پیام محلی از `ink-500` استفاده می‌کنند؛ عنوان، متن و نقل‌قول روی برگ از `document-text` خوانده می‌شوند. سطح پیش‌نمایش گراف `paper-100`، کادر رندر `paper-50`، گره‌های عادی Mermaid برابر `input-bg` و گرهٔ منتخب و CTA برابر `proof-blue-soft` هستند؛ متن CTA و گرهٔ منتخب `proof-blue` است. سایهٔ برگ فقط `0 18px 54px shadow-color` با مقدار تاریک Foundation باقی می‌ماند و هیچ border تزئینی به سطوح خنثی افزوده نمی‌شود. انتخاب تم قبل از ورود Reading حفظ و در راه‌اندازی بعدی از ذخیره‌سازی محلی بازیابی می‌شود.

در R03، انتخاب «جست‌وجو در متن» از نخستین کنترل Reading Rail یک پنل عادی و docked برابر ۳۰۴px میان Workspace و Rail فیزیکی راست باز می‌کند؛ این پنل overlay نیست و در قاب ۱۱۸۰×۸۵۸، Workspace را از ۱۱۲۴ به ۸۲۰px reflow می‌دهد تا برگ ۷۶۰px دقیقاً در x=۳۰ دوباره مرکز شود. خود Rail برابر ۵۶px در x=۱۱۲۴ ثابت می‌ماند و کنترل Search در حالت active از `paper-50` و `ink-950` استفاده می‌کند. پنل `paper-200` با گوشه‌های چپ ۱۴px، inset چهارده‌پیکسلی و separator یک‌پیکسلی در مرز Workspace است؛ Header آن ۲۷۶×۵۲px، عنوان تک‌ردیف ۱۲/۱۸ «جست‌وجو در متن» و دو کنترل ۳۶px بی‌مرز برای تمرکز Search و جمع‌کردن پنل دارد. آیکن جمع‌کردن Material Symbol برابر `left_panel_open` است و با بستن اشتباه گرفته نمی‌شود.

جست‌وجوی R03 فقط روی متن رندرشدهٔ همان سند فعال و کاملاً محلی انجام می‌شود و از index کتابخانه، فایل‌های Recent یا شبکه استفاده نمی‌کند. ورودی ۲۷۶×۴۰px با `paper-50`، شعاع ۶px و placeholder «جست‌وجو در متن سند…» در حالت خنثی border ندارد؛ Focus تنها حالت خنثی دارای stroke است. نتیجه‌ها با شمارندهٔ «n نتیجه در همین سند» و ردیف‌های ۲۷۶×۴۰px بدون آیکن تکراری می‌آیند؛ عنوان هر نتیجه به نزدیک‌ترین تیتر معنایی سند نسبت داده می‌شود، شمارندهٔ `current/total` در چپ فیزیکی قرار می‌گیرد و نتیجهٔ فعال فقط با `proof-blue-soft` و `proof-blue` مشخص می‌شود. Enter و ArrowDown نتیجهٔ بعد، Shift+Enter و ArrowUp نتیجهٔ قبل را به‌شکل حلقوی فعال می‌کنند؛ Escape پنل را جمع و focus را به trigger واقعی Rail برمی‌گرداند. query هنگام جمع‌کردن پنل حفظ می‌شود، نتیجهٔ فعال در متن با Highlight API نشان داده می‌شود و تمام بازشدن‌ها، بستن‌ها و پرش‌ها anchor معنایی، selection و scroll مطالعه را بدون تغییر محتوا حفظ می‌کنند. در عرض ۸۲۰px و کمتر همین محتوا در Drawer modal موجود با target حداقل ۴۴px ارائه می‌شود.

در R04، انتخاب «فهرست سند» از Reading Rail همان هندسهٔ docked و reflow دقیق R03 را نگه می‌دارد. Header با عنوان تک‌ردیف ۱۲/۱۸ «فهرست سند» و دو کنترل ۳۶px بی‌مرز برای جست‌وجوی محلی تیترها و جمع‌کردن پنل می‌آید. خلاصهٔ «ساختار سند · n بخش» با ۱۲/۱۸ و وزن ۷۰۰ در ابتدای body قرار می‌گیرد و footer دقیقاً «محلی · فقط تیترهای همین سند» است.

فهرست R04 مستقیماً از تیترهای واقعی همان سند ساخته می‌شود، سه سطح سلسله‌مراتبی را با تورفتگی ۱۰px نشان می‌دهد و شماره‌گذاری معنایی «۰۱، ۰۲، ۲٫۱…» دارد. ردیف‌ها ۲۷۶×۴۰px، با شعاع ۶px، بدون border، shadow یا آیکن تکراری هستند؛ فقط Focus حلقه می‌گیرد و ردیف فعال با `proof-blue-soft` و `proof-blue` مشخص می‌شود. click یا Enter به تیتر مربوط در سطح فعال سند می‌پرد، ArrowUp/ArrowDown و Home/End فوکوس ردیف‌ها را جابه‌جا می‌کنند، Escape پنل را جمع و focus را به Rail برمی‌گرداند. scroll spy مطالعه ردیف فعال را با تیتر دیده‌شده همگام و در محدودهٔ دید نگه می‌دارد؛ جست‌وجو فقط تیترهای همین سند را فیلتر می‌کند. در عرض ۸۲۰px و کمتر، همین محتوا در Drawer modal موجود و با target حداقل ۴۴px ارائه می‌شود.

در R05، مقصد «هایلایت‌ها» همان هندسهٔ docked، reflow و Header تک‌ردیف R03/R04 را حفظ می‌کند. Header دو کنترل ۳۶px بی‌مرز برای جست‌وجوی محلی هایلایت‌ها و جمع‌کردن پنل دارد؛ خلاصهٔ «هایلایت‌ها · n مورد» با ۱۲/۱۸ و وزن ۷۰۰ در ابتدای body و footer دقیق «محلی · همراه فایل .ravi» است. ردیف‌ها ۲۷۶×۶۴px، با شعاع ۶px، بدون border، shadow یا آیکن تکراری‌اند و نقل‌قول ۱۲/۱۸، محل واقعی در ساختار سند ۱۱/۱۷ و زمان محلی ۱۱/۱۷ را نمایش می‌دهند. ردیف فعال فقط با `proof-blue-soft` و `proof-blue` مشخص می‌شود و Focus تنها مرز حالت خنثی است.

فهرست R05 مستقیماً از annotationهای واقعی نوع `highlight` همان سند ساخته می‌شود؛ margin یا دستهٔ دیگری وارد آن نمی‌شود. انتخاب ردیف به متن نقل‌شده در Reading می‌پرد و scroll spy، ردیف فعال را با نزدیک‌ترین هایلایت به آستانهٔ مطالعه همگام نگه می‌دارد. جست‌وجو فقط نقل‌قول و محل همین هایلایت‌ها را فیلتر می‌کند؛ ArrowUp/ArrowDown و Home/End فوکوس را جابه‌جا می‌کنند و Escape پنل را جمع و focus را به trigger واقعی Rail بازمی‌گرداند. حذف از مسیر آشکارشوندهٔ hover/focus یا کلید Delete/Backspace یک soft-delete نه‌ثانیه‌ای است؛ واگرد، ترتیب annotation، anchor مطالعه و focus ردیف بازیابی‌شده را برمی‌گرداند. در عرض ۸۲۰px و کمتر همین محتوا در Drawer modal موجود ارائه می‌شود و کنترل‌های جست‌وجو و حذف target حداقل ۴۴px دارند.

در R06، مقصد «نظرات» همان هندسهٔ docked، reflow، Header تک‌ردیف و footer محلی R05 را حفظ می‌کند. Header دو کنترل ۳۶px بی‌مرز برای جست‌وجوی محلی نظرات و جمع‌کردن پنل دارد؛ خلاصهٔ «نظرات · n مورد» با ۱۲/۱۸ و وزن ۷۰۰ در ابتدای body و footer دقیق «محلی · همراه فایل .ravi» است. هر ردیف ۲۷۶×۶۴px، با شعاع ۶px، بدون border، shadow یا آیکن تکراری است و نقل‌قول ۱۲/۱۸، متن قابل‌ویرایش نظر ۱۱/۱۷ و زمان محلی ۱۱/۱۷ را در دو سطر فشرده نشان می‌دهد. ردیف فعال `proof-blue-soft` می‌گیرد، اما فقط نقل‌قول آن `proof-blue` است؛ متن نظر و زمان رنگ خنثی خود را نگه می‌دارند. در تم تاریک، مقصد فعال Reading Rail به‌جای سطح روشن از `chrome-700` استفاده می‌کند.

فهرست R06 فقط از annotationهای واقعی نوع `comment` همان سند ساخته می‌شود؛ هایلایت، حاشیه، یادداشت یا legend دسته‌ها وارد آن نمی‌شوند. کلیک یا Enter روی نقل‌قول به متن مربوط می‌پرد و scroll spy نزدیک‌ترین نظر را با موقعیت Reading همگام می‌کند. متن نظر مستقیماً در همان ردیف و روی source واقعی annotation ویرایش و محلی ذخیره می‌شود؛ Focus تنها مرز حالت خنثی است و Escape داخل فیلد focus را به عمل پرش همان ردیف بازمی‌گرداند، نه اینکه پنل را ببندد. ساخت نظر در خود پنل عمل مستقل ندارد و فقط پس از انتخاب متن از ابزار انتخاب ممکن است. جست‌وجو نقل‌قول و متن نظر را فیلتر می‌کند؛ ArrowUp/ArrowDown و Home/End میان نقل‌قول‌ها حرکت می‌کنند و Escape پنل را جمع و focus را به trigger واقعی بازمی‌گرداند. حذف و واگرد همان صف مستقل نه‌ثانیه‌ای R05 را با حفظ ترتیب، متن و focus به‌کار می‌گیرند. در دسکتاپ، دو کنترل متنی ردیف حداقل target ۲۴px دارند؛ در عرض ۸۲۰px و کمتر ردیف برای دو target مستقل ۴۴px به ۸۸px افزایش می‌یابد و همین محتوا در Drawer modal موجود ارائه می‌شود.

در R07، ellipsis «ابزار مطالعه» فقط Popover اندازهٔ متن را باز می‌کند و جست‌وجو، فهرست، هایلایت و نظر در آن تکرار نمی‌شوند؛ این چهار مقصد منحصراً در Reading Rail باقی می‌مانند. در قاب ۱۱۸۰×۸۵۸، Popover دقیقاً در مختصات ۳۲×۸۶ با اندازهٔ ۲۳۲×۶۸px، padding دوازده‌پیکسلی و gap هشت‌پیکسلی قرار می‌گیرد؛ سطح `input-bg`، شعاع ۱۴px، بدون border و با تنها سایهٔ `0 14px 34px shadow-color` است. کنترل‌های دو سوی آن Material Symbols Rounded رسمی `text_decrease` و `text_increase` با glyph هجده‌پیکسلی و target دسکتاپ ۳۶×۳۶px هستند؛ مقدار میانی ۸۰×۲۴px با Label برابر ۱۲/۱۸، وزن ۷۰۰ و `ink-700` نمایش داده می‌شود.

اندازهٔ Reading در R07 فقط با گام یک‌پیکسلی میان ۱۶ تا ۲۲ تغییر می‌کند؛ در هر انتهای بازه کنترل متناظر با `disabled` واقعی و opacity مرجع ۴۲٪ غیرفعال می‌شود. تغییر اندازه همان سند و همان source را نگه می‌دارد و anchor معنایی، scroll، selection و focus مطالعه را بدون transaction محتوایی بازیابی می‌کند. Arrowها میان کنترل‌های فعال حرکت می‌کنند، Home/End ابتدا و انتها را می‌گیرند، Escape Popover را می‌بندد و focus را به ellipsis بازمی‌گرداند و کلیک بیرون فقط Popover را جمع می‌کند. در عرض ۸۲۰px و کمتر targetها ۴۴px می‌شوند و Reading Rail چهارآیتمی در حالت بسته در راست فیزیکی قابل‌دسترسی می‌ماند؛ انتخاب مقصد، همان محتوای R03 تا R06 را در Drawer modal باز می‌کند.

در R08، Palette انتخاب متن فقط پس از یک selection واقعی و غیرخالی روی متن رندرشده ظاهر می‌شود و دقیقاً دو عمل دارد: «هایلایت» و «نظر». «کپی» همچنان رفتار بومی سیستم و `Ctrl+C` است اما کنترل دیداری Palette نیست؛ «حاشیه» نیز در این سطح حضور ندارد. Palette در دسکتاپ ۲۴۸×۵۲px با padding هشت‌پیکسلی، gap هشت‌پیکسلی، `input-bg`، شعاع ۱۴px، بدون border و با سایهٔ `0 6px 16px shadow-color` است. عمل‌ها هرکدام ۱۰۸×۳۶px، شعاع ۶px، Label برابر ۱۲/۱۸ و glyph رسمی هجده‌پیکسلی Material Symbols دارند؛ «هایلایت» از `highlight-bg`/`ink-950` و «نظر» از `comment-highlight-bg`/`comment-blue` استفاده می‌کند. focus تنها با حلقهٔ داخلی دوپیکسلی `focus-ring` نشان داده می‌شود و ابعاد کنترل را تغییر نمی‌دهد.

مرکز Palette با مرکز محدودهٔ انتخاب هم‌راستا و لبهٔ پایین آن، وقتی فضای بالا کافی است، به لبهٔ بالای selection متصل می‌شود؛ نزدیک لبهٔ بالا همین رابطه زیر selection معکوس می‌شود و در هر دو حالت Palette در عرض Preview clamp می‌ماند. خود بازخورد انتخاب در هر دو تم فقط با یک لایهٔ گوشه‌گرد از رنگ مرجع `#f8e69d` با opacity برابر ۷۲٪ ساخته می‌شود؛ paint بومی `::selection` و Custom Highlight در این حالت شفاف‌اند تا رنگ دوباره روی خودش ترکیب نشود. این محدوده border، underline یا سایهٔ آبی ندارد. ArrowLeft/ArrowDown در ترتیب RTL از «هایلایت» به «نظر» و ArrowRight/ArrowUp در جهت عکس حرکت می‌کنند؛ Home/End ابتدا و انتها را می‌گیرند. Escape، کلیک بیرون، selection جمع‌شده یا resize Palette و بازخورد انتخاب را می‌بندد. `Ctrl+C` رفتار بومی سیستم است و تا وقتی selection لغو نشده، Palette و محدوده را دست‌نخورده نگه می‌دارد. «هایلایت» annotation واقعی را ثبت و مقصد هایلایت‌ها را باز می‌کند؛ «نظر» composer واقعی را بدون reflow ناخواسته باز می‌کند و لغو آن focus را به عمل «نظر» بازمی‌گرداند. در عرض ۸۲۰px و کمتر ارتفاع عمل‌ها ۴۴px و ارتفاع Palette برابر ۶۰px است.

در R09، fenced block واقعی Mermaid در Reading با همان renderer، cache، متن جایگزین و SVG موجود به یک Preview برابر ۶۶۴×۱۷۲px تبدیل می‌شود؛ تصویر نمایشی یا renderer دوم مجاز نیست. سطح بیرونی `paper-100`، شعاع ۱۴px و بدون border یا shadow است. عنوان معنایی نمودار در ۱۶px بالا و راست با Label برابر ۱۲/۱۸ و metadata ثابت «Mermaid · رندر شده در سند» با Caption برابر ۱۱/۱۷ می‌آید. سطح رندر داخلی دقیقاً در inset افقی ۱۶px و y=72، برابر ۶۳۲×۸۴px، `paper-50` و شعاع ۶px است. کنترل اختیاری «تمام‌صفحه» در x=16 و y=16 برابر ۱۴۲×۳۶px، با padding افقی ۱۰px، gap شش‌پیکسلی، glyph رسمی هجده‌پیکسلی Material Symbols `fullscreen` و Label برابر ۱۲/۱۸ است؛ حالت Focus با حلقهٔ داخلی دوپیکسلی `focus-ring` اندازه را تغییر نمی‌دهد.

فراخوانی «تمام‌صفحه» همان instance و همان `data-mermaid-render-key` و Blob URL را وارد Graph Viewer می‌کند؛ Studio باز نمی‌شود و کپی دوم SVG ساخته نمی‌شود. Viewer یک dialog واقعی با Zoom، Fit، Pan و Escape است؛ هنگام ورود anchor معنایی و scroll دقیق Reading ثبت می‌شود و خروج، همان anchor، scroll و focus کنترل مبدأ را برمی‌گرداند. دوبارکلیک گراف در Reading همین مسیر Viewer را فراخوانی می‌کند، اما ویرایش گراف فقط از Graph Studio در سطح نوشتن انجام می‌شود. در عرض ۸۲۰px و کمتر Preview برای جلوگیری از هم‌پوشانی metadata و کنترل به ۲۲۸px افزایش ارتفاع می‌دهد؛ CTA لمسی ۴۴px و سطح رندر همچنان ۸۴px باقی می‌ماند و هیچ اسکرول افقی ایجاد نمی‌شود.

در R10، Graph Viewer یک سطح مستقل و همواره تیره با اندازهٔ viewport است و از theme جاری سند رنگ نمی‌گیرد. در قاب مرجع ۱۱۸۰×۸۵۸، هدر `paper-50` دقیقاً ۷۲px ارتفاع دارد؛ عنوان «نام نمودار · نام فایل» در x=500 و y=19 با عرض ۶۴۰px و Heading/H3 برابر ۲۳/۳۵ رندر می‌شود. کنترل کپسولی «بازگشت به سند» در x=20 و y=14 برابر ۱۶۴×۴۴px، `input-bg`، شعاع کامل، gap هشت‌پیکسلی، glyph رسمی `arrow_back` هجده‌پیکسلی و سایهٔ Floating برابر `0 12px 30px shadow-color` است. بازگشت با این کنترل یا Escape، focus، scroll و anchor معنایی Reading را دقیقاً بازیابی می‌کند.

Canvas از y=72 تا انتهای viewport با `editor-surface-bg` ادامه دارد و drag برای Pan روی تمام آن فعال است. Toolbar در x=24 و y=24 Canvas برابر ۱۸۴×۵۲px، `input-bg`، padding هشت‌پیکسلی، gap هشت‌پیکسلی، شعاع شش‌پیکسلی و بدون border یا shadow است؛ چهار کنترل borderless آن به‌ترتیب فیزیکی Zoom out، Zoom in، Fit و Fullscreen، هرکدام ۳۶×۳۶px با Material Symbols رسمی هجده‌پیکسلی هستند. Fullscreen این toolbar فقط Fullscreen بومی سیستم را toggle می‌کند و خود Viewer را نمی‌بندد. Preview مرکزی در x=160 و y=270 Canvas برابر ۸۶۰×۲۲۳px، `paper-100` و شعاع ۱۴px است؛ محتوای R09 در پهنای اصلی ۶۶۴px آن حفظ می‌شود، یعنی metadata در x=348، CTA در x=16 و سطح همان SVG در x=16 و y=72 با اندازهٔ ۶۳۲×۸۴px قرار می‌گیرد و سمت راست Preview عمداً فضای آرام باقی می‌ماند. در موبایل هدر ۶۴px، کنترل بازگشت icon-only با target برابر ۴۴px، toolbar با چهار target برابر ۴۴px و Preview برابر عرض viewport منهای ۲۴px است؛ هیچ اسکرول افقی مجاز نیست.

در desktop اندازهٔ دیداری کنترل‌های chrome ۳۶px با آیکن‌های ۱۶–۱۸px است. روی ورودی لمسی target واقعی حداقل ۴۴px می‌ماند. tooltip هر عمل در hover و focus صفحه‌کلیدی ظاهر می‌شود. در عرض ۳۲۰px فقط برند، «باز کردن فایل» با برچسب کامل، ذخیره و overflow در نوار باقی می‌مانند؛ بنابراین عمل آغازین پشت منو پنهان نمی‌شود.

### قرارداد sidebar تک‌سندی

sidebar یک سطح پشتیبان در سمت راست فیزیکی است، نه workspace دوم. rail ثابت ۵۶px هفت مقصد محصولی «کتابخانه»، «تاریخچه»، «نسخه‌ها»، «اصلاحات فارسی»، «فهرست سند»، «هایلایت‌ها» و «نظرات» را به همین ترتیب نگه می‌دارد. «کتابخانه» و «Recent/تاریخچه» دو مقصد جدا هستند؛ تاریخچه یعنی فایل‌های بازشده بر اساس زمان و نسخه‌ها یعنی نسخه‌های قابل‌بازگشت سند فعال. active state با `aria-current`، سطح پس‌زمینه و آیکن Filled بیان می‌شود و فقط به رنگ وابسته نیست. کنترل neutral در حالت عادی border ندارد.

pane از grammar مشترک عنوان محلی، actionهای همان view، فیلتر/جست‌وجو، محتوای ردیفی و status کم‌صدا پیروی می‌کند. عرض آن بین ۲۴۰ تا ۴۲۰px با mouse و separator صفحه‌کلیدی تغییر می‌کند؛ view فعال، عرض و collapsed state محلی ذخیره می‌شوند. rail و جهت فضایی shell همواره LTR و در راست فیزیکی‌اند، اما متن، نام فایل و ردیف‌های pane بر پایهٔ `dir="auto"` یا RTL فارسی رندر می‌شوند. تغییر view هرگز سند فعال را عوض یا editor را remount نمی‌کند.

در P01، مقدار پیش‌فرض pane دقیقاً ۳۰۴px است و همراه rail پنجاه‌وشش‌پیکسلی یک ستون ۳۶۰px در راست فیزیکی می‌سازد؛ resize handle پنج‌پیکسلی روی separator قرار می‌گیرد و عرض مستقلی از workspace مصرف نمی‌کند. در قاب ۱۲۸۰×۹۱۴، pane از x=920 و y=92 تا ارتفاع ۸۲۲ و rail از x=1224 آغاز می‌شوند؛ Tab Bar در بالای Stage قرار می‌گیرد و سند ۷۶۰×۷۵۴ در x=80 و y=144 است. سطح pane برابر `paper-200`، سطح rail برابر `paper-100` و separator یک خط `rule` در لبهٔ چپ فیزیکی pane است.

پنل کتابخانه ۱۴px padding و gap هشت‌پیکسلی دارد. Header یک ردیف ۲۷۶×۵۲px است: عنوان Label/Desktop برابر ۱۲/۱۸ در راست و دو کنترل borderless سی‌وشش‌پیکسلی Search و PanelCollapse در چپ؛ Collapse پنل را جمع می‌کند و معنای Close ندارد. Header با یک hairline در پایین جدا می‌شود. ردیف «پوشه‌ها» ۲۷۶×۴۰px با `paper-100` و شعاع پنج‌پیکسلی است و دو عمل FolderAdd و Refresh را در چپ فیزیکی نگه می‌دارد.

ردیف‌های درخت ۲۸۰×۳۶px، Label/Desktop ۱۲/۱۸، metadata برابر Caption ۱۱/۱۷ و glyph رسمی Material Symbols هجده‌پیکسلی دارند. حالت neutral border ندارد؛ Hover فقط `paper-100` می‌گیرد و Selected با `proof-blue-soft` و stroke داخلی یک‌پیکسلی `proof-blue` مشخص می‌شود. منوی سه‌نقطه فقط در hover، focus یا ردیف فعال دیده می‌شود. پایان pane تنها یک خط Caption مرکزی «فقط روی این دستگاه · فایل‌ها به اینترنت ارسال نمی‌شوند» دارد؛ status یا ردیف حریم خصوصی دوم در P01 مجاز نیست.

در عرض ۱۲۴۰px و کمتر، rail و pane با همان معماری به drawer سمت راست تبدیل می‌شوند. drawer دارای scrim، `role="dialog"`، `aria-modal`، focus containment، Escape/Back و بازگرداندن focus به trigger واقعی است؛ تمام targetهای لمسی حداقل ۴۴px هستند. بسته‌شدن drawer یا pane فقط سطح context را جمع می‌کند و موقعیت و scroll سند را تغییر نمی‌دهد.

در حالت مطالعه، برگ خواندن عنصر اصلی است و rail اختصاصی فقط چهار مقصد «جست‌وجو در متن»، «فهرست سند»، «هایلایت‌ها» و «نظرات» را نشان می‌دهد. pane از سمت راست به‌شکل عادی و در جریان layout باز می‌شود، نه overlay؛ در حالت جمع‌شده فقط rail باقی می‌ماند. هدر ثابت نام فایل و بازگشت به میز را نشان می‌دهد، هنگام اسکرول رو به پایین از قاب خارج می‌شود و با اسکرول رو به بالا بازمی‌گردد. روی موبایل همین پنل به drawer تبدیل می‌شود تا عرض خواندن ثابت بماند.

در عرض ۸۲۰px و کمتر، دو سطح به تب‌های «ویرایش» و «پیش‌نمایش» تبدیل می‌شوند. نوار وضعیت دو ردیفه می‌شود، اکشن بازکردن فایل برچسب خود را حفظ می‌کند، کنترل‌های کم‌کاربرد به overflow می‌روند و جدول‌ها به‌تنهایی امکان پیمایش افقی دارند.

## Elevation & Depth

سیستم در حالت کار تخت است و عمق را ابتدا با اختلاف سطح رنگ می‌سازد. کنترل‌های neutral، rail و کارت Empty State در حالت عادی border ندارند؛ مرز یک‌پیکسلی فقط برای ورودی، focus/validation، اکشن تأکیدی یا جداسازی ضروری مجاز است. فقط برگ حالت مطالعه سایه‌ی پایین‌افتاده‌ی نرم دارد (`0 18px 54px rgba(29, 36, 30, 0.17)`). بازخورد کشیدن فایل سایه‌ی قوی‌تر و موقت می‌گیرد.

تمام سایه‌های مجاز از tokenهای معنایی `--shadow-*` می‌آیند. رنگ‌های چاپ نیز با `--print-ink`، `--print-paper` و `--print-rule` تعریف می‌شوند تا خروجی چاپی literal پراکنده نداشته باشد.

**The Flat Workbench Rule.** کنترل‌ها و پنل‌های کاری در حالت عادی سایه ندارند؛ سایه فقط برای برگ مطالعه، اعلان و بازخورد جابه‌جایی فایل است.

## Shapes

پنل‌ها گوشه‌ی مستقیم دارند. کنترل‌های قالب‌بندی شعاع ۴ پیکسلی و دکمه‌های عملیاتی شعاع ۶ پیکسلی دارند. نشان ثبت و نقطه‌ی وضعیت تنها دایره‌های کامل سیستم‌اند؛ هیچ کنترل متنی به کپسول تزئینی تبدیل نمی‌شود.

شعاع کامل فقط با token `--radius-pill` برای switch thumb، badge فشرده، progress mark و وضعیت‌های ذاتاً دایره‌ای مجاز است؛ این token مجوز ساخت CTA کپسولی نیست.

## Components

مودال ساخت فایل جدید از همان منطق «برگهٔ ثبت» مودال ذخیره استفاده می‌کند، اما نام را به‌صورت پایه و پسوند را به‌صورت مهر ثابت و غیرقابل‌ویرایش نشان می‌دهد. خطای نام دقیقاً زیر فیلد، هشدار تغییرات ذخیره‌نشده پیش از فرم و نتیجهٔ نهایی نام و محل ذخیره پیش از عمل اصلی نمایش داده می‌شوند؛ عمل ساخت تا رفع خطای نام غیرفعال می‌ماند.

### Buttons

- **Shape:** مستطیل کم‌گرد (۶px) با ارتفاع ۴۲px
- **Primary:** آبی نمونه‌خوان با متن سفید و وزن ۷۰۰
- **Hover / Focus:** تیره‌شدن آبی، یک جابه‌جایی یک پیکسلی و حلقه‌ی فوکوس سه پیکسلی
- **Quiet:** شفاف با خط خاکستری روی نوار مرکب؛ در حالت فعال به کاغذ سفید تبدیل می‌شود

آیکن «افزودن تصویر» در گروه قالب‌بندی ادیتور و پیش از نمودار قرار می‌گیرد تا در
کنار پیوند و پیش از ابزارهای ساخت محتوا دیده شود. با زدن آن یک مدال کم‌حجم با دو
زبانهٔ «فایل محلی» و «نشانی اینترنتی» باز می‌شود. هر دو مسیر فقط PNG، JPEG، WebP
و GIF را می‌پذیرند. فایل محلی به محمولهٔ داخلی راوی تبدیل می‌شود، اما مسیر URL
فقط خود نشانی را با اقدام «درج نشانی» در Markdown نگه می‌دارد؛ در نتیجه فایل سبک
می‌ماند و متن مدال وابستگی نمایش به اینترنت را شفاف می‌گوید.

### Cards / Containers

- **Corner Style:** مستقیم
- **Background:** کاغذ سفید برای پیش‌نمایش و کاغذ کار برای ویرایش
- **Shadow Strategy:** بدون سایه در میز کار
- **Border:** خط صحافی یک پیکسلی
- **Internal Padding:** وابسته به نقش؛ ۱۴px برای ابزار و ۲۲ تا ۷۲px برای نوشته

### Inputs / Fields

- **Style:** textarea تمام‌سطح با خطوط مبنای ۳۲ پیکسلی و بدون قاب داخلی
- **Focus:** خط داخلی دو پیکسلی آبی
- **Error / Disabled:** خطا روی کاغذ قرمز کم‌رنگ؛ کنترل غیرفعال با مرکب بسیار روشن

### قرارداد جست‌وجو و جایگزینی محلی

`Ctrl/Cmd+F` نوار Find را در بالای همان سطح ۷۶۰پیکسلی سند باز می‌کند؛ در Windows/Linux میان‌بر `Ctrl+H` و در macOS میان‌بر `Cmd+Option+F` همان سطح را به Replace گسترش می‌دهد. Find در دسکتاپ ۷۶۰×۱۰۰px و Replace برابر ۷۶۰×۱۹۲px است: padding هشت‌پیکسلی، شعاع ۱۴px، زمینهٔ `input-bg`، بدون border تزئینی و با سایهٔ Popover. هر ردیف ۸۴px است؛ Search Field و Text Field جایگزینی هرکدام ۳۲۰×۸۴px و کنترل داخلی آن‌ها ۳۶px است. گزینه‌های Case، Whole Word و RegExp به‌شکل Pill و تمام آیکن‌ها Material Symbols هجده‌پیکسلی‌اند.

جست‌وجو و جایگزینی فقط روی متن محلی سند فعال انجام می‌شود. `F3` و `Shift+F3` میان نتیجه‌ها حرکت می‌کنند، `Enter` در فیلد جایگزینی نتیجهٔ جاری و `Alt+Enter` همهٔ نتیجه‌ها را جایگزین می‌کند. Replace All یک transaction واحد Undo می‌سازد؛ شمارنده باید کل نتایج را بدون سقف جعلی گزارش کند و در سندهای بزرگ از cache وابسته به نسخهٔ متن و query استفاده کند. در موبایل دو ردیف به sheet پشته‌ای تبدیل می‌شوند و input، clear و همهٔ actionها target واقعی حداقل ۴۴px دارند.

### Navigation

دکمهٔ «حمایت» با قلب مرجانی در گروه عمل‌های هدر قرار می‌گیرد و در عرض کوچک
به آیکن ۴۴ پیکسلی تبدیل می‌شود. صفحهٔ حمایت یک ورودی متمرکز و دسترس‌پذیر است:
وعدهٔ «رایگان، همیشه» روی کاغذ حاشیه‌نویسی مرجانی، توضیح کوتاه هزینه‌های پروژه،
عمل اصلی دارمت، مسیر کانال بروزرسانی‌ها و وب‌سایت رسمی. مبلغ و متن پرداخت
پیش‌فرض نمی‌شوند؛ کاربر آن‌ها را در دارمت انتخاب می‌کند. یادآوری ثبت نام حامی
باید کنار عمل پرداخت دیده شود و فهرست حامیان با رضایت آن‌ها در شناسنامهٔ محصول
ثبت شود.

نوار بالا مرکب عمیق است و نام را با یک مهر مربع و علامت برش معرفی می‌کند. برند در chrome نقش هویت دارد و عمل «درباره» نیست؛ دربارهٔ محصول در overflow قرار می‌گیرد. در موبایل اکشن برچسب‌دار «باز کردن فایل» حفظ می‌شود و ذخیره و overflow با آیکن استاندارد و نام دسترس‌پذیر باقی می‌مانند.
مهر و نام راوی یک کنترل واحد برای بازکردن شناسنامهٔ محصول‌اند. شناسنامه مانند دفتر ثبت نسخه طراحی می‌شود: هدر مرکبی با مهر و شمارهٔ نسخه، معرفی کوتاه روی کاغذ آبی نمونه‌خوان، فهرست خطی ویژگی‌های متمایز و دفتر تغییرات جمع‌شونده. نسخه باید مستقیماً از اطلاعات بستهٔ برنامه خوانده شود و مودال در هر دو تم، موبایل و صفحه‌کلید همان قواعد فوکوس مودال‌های اصلی را رعایت کند.
سوییچ روشن/تاریک یک کنترل گرد و خنثی ۲۲×۲۲ پیکسلی در خوشهٔ هویت مرکزی Title Bar است؛ در حالت عادی و فعال border یا ظاهر Selected نمی‌گیرد، نام دسترس‌پذیر آن مقصد تغییر («فعال‌کردن تم تاریک/روشن») را بیان می‌کند و با `Alt+T` نیز قابل فراخوانی است.

کتابخانه از الگوی سایدبار کم‌مزاحمت استفاده می‌کند: عمل سنجاق در انتهای هر ردیف و فقط هنگام hover یا focus نمایان می‌شود، اما سنجاق فعال همیشه دیده می‌شود. روی دستگاه لمسی کنترل سنجاق همواره قابل مشاهده است. فایل‌های سنجاق‌شده در بخش مستقل «سنجاق‌شده‌ها» در ابتدای کتابخانه تکرار می‌شوند و نسخهٔ اصلی آن‌ها در ساختار پوشه باقی می‌ماند. آخرین سنجاق بالاتر قرار می‌گیرد و ارتفاع بخش برای مجموعه‌های بزرگ محدود و پیمایش‌پذیر است.

### قرارداد کاوشگر فایل محلی

قفسه فقط نمایی از یک پوشهٔ انتخاب‌شده با اجازهٔ صریح کاربر است؛ اصطلاح vault، metadata پنهان یا شبکهٔ دانش وارد مدل نمی‌شود. ریشه، پوشه و فایل شناسهٔ پایدار مبتنی بر root و مسیر نسبی دارند و همان قرارداد معنایی برای Web، Electron و adapter احتمالی Tauri به‌کار می‌رود. renderer هرگز با مسیر مطلق دلخواه اختیار نوشتن نمی‌گیرد؛ adapter بومی root انتخاب‌شده، path traversal، symlink خروجی، نام رزروشده و مقصد موجود را دوباره اعتبارسنجی می‌کند.

tree برای مجموعه‌های بزرگ به ردیف‌های تخت تبدیل و فقط viewport با overscan محدود رندر می‌شود. پوشه‌ها expand/collapse می‌شوند، indentation کم‌عمق است، مسیر کامل در tooltip و نام با `dir="auto"` و `unicode-bidi: plaintext` نمایش داده می‌شود. تغییر فایل فعال فقط source همان editor موجود را عوض می‌کند؛ tab یا editor دوم ساخته نمی‌شود. reveal فقط وقتی ردیف فعال بیرون viewport باشد scroll را جابه‌جا می‌کند و در حالت عادی موقعیت کاوشگر ثابت می‌ماند.

عملیات create، rename، move و delete از منوی متمرکز هر ردیف انجام می‌شود. فایل active/dirty پیش از rename، move یا delete مسدود می‌ماند تا ذخیره شود. حذف در Desktop ابتدا به trash داخلی قابل‌بازگشت منتقل می‌شود؛ در Web پیش از حذف snapshot محدود و محلی گرفته می‌شود. Undo هرگز فایل تازه‌ای را overwrite نمی‌کند. تغییر یا حذف بیرونی فایل، متن حاضر روی میز را جایگزین نمی‌کند و یک پیام عملیاتی برای بازخوانی، ذخیره با نام تازه یا اتصال دوباره نشان می‌دهد.

### قرارداد بازیابی محلی

نمای «جست‌وجو» محتوای قفسه را با یک نمایهٔ افزایشی و قابل‌لغو روی همان دستگاه می‌خواند. نام، مسیر، snippet، شمارهٔ خط و محدودهٔ match فقط در حافظهٔ برنامه پردازش می‌شوند و خواندن نمایه نباید فایل را بازشده حساب کند، history بسازد یا محتوایی به server بفرستد. تغییر scan، rename و delete منابع نمایه و recentهای وابسته به قفسه را در همان چرخه پاک‌سازی می‌کند.

`Quick Open` مسیر جدا و keyboard-first برای نام فایل است: `Ctrl/⌘+P` آن را باز می‌کند، fuzzy match را با تازگی، تکرار و سنجاق محلی ترکیب می‌کند و نتیجه را در همان editor موجود جایگزین می‌کند. ردیف‌های نتیجه، recent و pinned یک grammar مشترک دارند؛ آیکن نوع فایل، نام با جهت طبیعی، مسیر، metadata کم‌صدا و وضعیت فعال غیررنگی. جست‌وجوی محتوا و Quick Open هیچ tab، سند دوم، query operator یا semantic/AI search ایجاد نمی‌کنند.

### قرارداد فرمان‌های راوی

`command-registry` منبع واحد شناسه، عنوان فارسی، توضیح، alias انگلیسی، گروه، آیکن، میان‌بر و زمینهٔ فرمان است و یک executor واحد handler و availability زمان اجرا را اعمال می‌کند. header، overflow، toolbar، میان‌بر، Palette و completion خطی `/` حق ندارند منطق فرمان را دوباره پیاده کنند. هر عمل اصلی قابل‌کلیک `data-command-id` پایدار دارد؛ حذف کنترل دیداری، قابلیت را حذف نمی‌کند.

Palette با `Ctrl/⌘+K` باز می‌شود، در گروه‌های فایل، ویرایش، نما و خواندن جست‌وجوی fuzzy فارسی/انگلیسی دارد و فرمان غیرفعال را حذف نمی‌کند؛ علت انسانی و قابل اقدام را در همان ردیف نشان می‌دهد. Arrow، Enter و Escape مسیر کامل صفحه‌کلید هستند و بستن یا اجرا focus را به مبدأ بازمی‌گرداند. سابقهٔ اجرا فقط در `localStorage` همان دستگاه برای ranking نگه‌داری می‌شود و هیچ telemetry شبکه‌ای وجود ندارد.

Palette و Quick Open دو وظیفهٔ جدا دارند اما از primitive مشترک suggestion row استفاده می‌کنند: آیکن، عنوان، توضیح، metadata، selected، active و disabled. در موبایل Palette یک سطح محافظت‌شده با target حداقل ۴۴px، safe-area، فهرست پیمایش‌پذیر و footer فشرده است؛ در دسکتاپ همان دستور زبان کاغذ/مرکب و آبی نمونه‌خوانی را حفظ می‌کند و به ظاهر IDE یا clone اوبسیدین تبدیل نمی‌شود.

### قرارداد هستهٔ ویرایش روان

«ویرایش روان»، «متن خام» و «نمونه‌خوانی دوبرگی» سه نمایش از یک سند Markdown هستند، نه سه ویرایشگر. نمونهٔ CodeMirror هنگام تغییر حالت remount نمی‌شود و تمام تغییرها فقط با transaction همان نمونه وارد source می‌شوند؛ HTML پیش‌نمایش یا decoration هرگز منبع ذخیره نیست. حالت و نسبت pane همراه نسخهٔ state محلی ذخیره می‌شوند و state قدیمی یا نامعتبر به ویرایش روان، یا در صورت خاموش‌بودن feature flag به متن خام، مهاجرت می‌کند.

ویرایش روان فقط syntax tree محدودهٔ قابل‌دیدن با یک خط overscan و خط فعال را decorate می‌کند. delimiter در خط فعال یا محدودهٔ selection پنهان نمی‌شود و پنهان‌سازی فقط decoration دیداری است. جهت هر خط با utility مشترک ۷۰٪ تعیین می‌شود؛ fenced code بدون توجه به متن، LTR و mono است. خطای parser یا decoration متن، history و selection را تغییر نمی‌دهد: extension به decoration خالی برمی‌گردد، یک‌بار خطا را گزارش می‌کند و پوسته «متن خام» را با پیام انسانی فعال می‌کند. متغیر `NEXT_PUBLIC_RAAVI_LIVE_EDIT=0|false|off|disabled` همین مسیر fallback را پیش از اجرا فعال می‌کند.

### قرارداد Markdown درون‌خطی

در «ویرایش روان»، heading، emphasis، strike، highlight، inline code، فهرست، task، quote و link با تایپوگرافی همان برگه رندر می‌شوند؛ source همچنان متن Markdown است. marker ساختاری فقط در خط فعال و delimiter درون‌خطی فقط وقتی cursor یا selection داخل همان span است آشکار می‌شود. decoration حق تغییر ارتفاع پایهٔ خط، تزریق متن ذخیره‌شونده یا ساخت history جدا ندارد. `==highlight==` یک extension متنی راوی است که در فایل به همان شکل قابل‌خواندن می‌ماند و Preview آن را با افزونهٔ remark محدود و بدون تبدیل source به `<mark>` نمایش می‌دهد.

task خارج خط فعال یک checkbox دسترس‌پذیر است و هر toggle دقیقاً یک transaction برای `[ ]` یا `[x]` می‌سازد. link فقط عنوان خوانا را نشان می‌دهد، URL در tooltip و popover ویرایش می‌شود و فقط مقصد `http/https` کنترل «بازکردن» می‌گیرد؛ مقصد نسبی برای حمل‌پذیری قابل ویرایش می‌ماند. image امن در Live به نمونهٔ چاپی دارای alt و state بارگیری/خطا تبدیل می‌شود، HTML comment در Live placeholder و در Preview نامرئی است و horizontal rule به خط چاپی قابل‌بازگشت به source تبدیل می‌شود.

folding heading و list فقط state محلی CodeMirror است و هیچ نشانه‌ای در فایل ذخیره نمی‌کند. Copy از editor همیشه Markdown خامِ selection را می‌نویسد؛ Copy انتخاب Preview فقط متن رندرشده را می‌نویسد. inline code و URL با `unicode-bidi: isolate` از متن فارسی جدا و LTR می‌شوند، اما جهت خود خط همچنان از utility مشترک سند پیروی می‌کند.

### قرارداد بلوک‌های غنی Live Edit

کد fenced، جدول GFM، تصویر، Callout محدود، پاورقی و Mermaid در محدودهٔ viewport به widget نتیجه‌محور تبدیل می‌شوند. این widgetها projection هستند: source Markdown، selection، history و clipboard بیرون از DOM آن‌ها می‌ماند و هر بلوک مسیر آشکار «ویرایش متن Markdown» دارد. decoration چندخطی از `StateField` و driver محدودهٔ قابل‌دیدن می‌آید تا قرارداد layout خود CodeMirror و scroll continuity حفظ شود.

کد جهت LTR و scroll افقی مستقل دارد؛ جدول semantic در region پیمایش‌پذیر است؛ تصویر فقط scheme امن یا asset همان `.ravi` را می‌پذیرد. Mermaid از renderer/cache موجود راوی استفاده می‌کند، Blob را هنگام teardown آزاد می‌کند، متن نمودار را به‌عنوان representation جایگزین نگه می‌دارد و در خطا آخرین SVG سالم را همراه پیام فارسی نگه می‌دارد. کنترل «بازکردن در استودیو» همان block موجود را ویرایش می‌کند، نه نسخه‌ای جدا از سند.

### Selection Mini Menu

پس از انتخاب متن در پیش‌نمایش، دقیقاً دو عمل «هایلایت» و «نظر» در Palette افقی R08 ظاهر می‌شوند؛ «حاشیه» از ساختار محصول حذف شده و «کپی» کنترل دیداری ندارد. مرکز Palette با مرکز selection هم‌راستا و لبهٔ آن به محدودهٔ انتخاب متصل می‌شود؛ بالا یا پایین قرارگرفتن تابع فضای موجود است و Palette هرگز از لبهٔ پنل بیرون نمی‌رود. کلیک بیرون، لغو انتخاب، تغییر اندازهٔ پنجره و Escape پالت را می‌بندند؛ مسیر صفحه‌کلید و `prefers-reduced-motion` الزامی است.

ادیتور همین الگو را با چهار عمل دارای برچسب کوتاه «پررنگ، مورب، یادداشت و پیوند» به‌کار می‌گیرد. active state از syntax tree و selection می‌آید، نه از کلاس حدسی. انتخاب با ماوس منو را در محل رهاشدن نشانگر باز می‌کند؛ انتخاب صفحه‌کلیدی جای امنی داخل ادیتور می‌گیرد. پالت با برخورد به لبه جابه‌جا می‌شود؛ تایپ، اسکرول، تغییر نما، کلیک بیرون یا Escape آن را می‌بندد و focus به ویرایشگر یا کنترل مبدأ بازمی‌گردد.

### قرارداد ابزار ویرایش contextual

نوار دائم ویرایش دقیقاً هفت عمل دارد: تیتر، پررنگ، مورب، پیوند، فهرست، کار و «بیشتر». target دیداری در دسکتاپ ۳۱px اما سطح تعاملی touch حداقل ۴۴px است. فرمان‌های ساختاری، undo/redo، جست‌وجو، outline و بازبینی حذف نمی‌شوند؛ در منوی «بیشتر» با سه گروه «ساختار، ویرایش، نما» و در Palette و slash command از همان Command ID در دسترس‌اند.

گاتر خط فعال یک کنترل «ساختار این خط» دارد که منوی block را برای تیتر، انواع فهرست، نقل‌قول، کد، Callout، جدول، تصویر و Mermaid باز می‌کند. منو براساس فضای pane clamp می‌شود، active block را به‌صورت غیررنگی با `aria-checked` بیان می‌کند و Escape focus را به همان کنترل گاتر برمی‌گرداند. اجرای همهٔ گزینه‌ها transaction همان CodeMirror است؛ هیچ helper حق ساخت history یا source دوم ندارد.

پیوند، جدول و تصویر popover سبک و non-modal دارند. پیوند متن انتخابی را نگه می‌دارد، جدول فقط تعداد ردیف/ستون را می‌پرسد و تصویر میان فایل محلی و URL امن انتخاب می‌دهد. مسیر `/` عنوان، alias فارسی/انگلیسی و زیرتوالی fuzzy را رتبه‌بندی می‌کند و توضیح کوتاه command را به‌عنوان preview نشان می‌دهد. این disclosure نباید کار پایه را پنهان کند: bold، link، task و image همواره بدون دانستن syntax از سطح اصلی یا یک منوی نام‌دار قابل کشف‌اند.

### قرارداد پیوستگی context و مطالعه

ویرایشگر، preview و Reading با anchor ساختاری مبتنی بر offset منبع هماهنگ‌اند؛ درصد scroll فقط fallback است. تغییر «ویرایش روان»، «متن خام»، «نمونه‌خوانی دوبرگی» و «مطالعه» باید block، cursor و selection را نگه دارد و هیچ transaction محتوایی نسازد. فهرست سند فقط در sidebar چپ قرار می‌گیرد و به surface فعال می‌رود؛ outline دوم، right sidebar و preview موازی ممنوع‌اند.

Reading chrome فقط نام فایل، فهرست، یک reveal ابزار و بازگشت به میز را در سطح اول نگه می‌دارد. در موبایل sidebar فهرست پیش‌فرض بسته و هنگام بازشدن drawer modal با scrim، inert، Escape/Back و focus restoration است. حذف annotation یک soft-delete نه‌ثانیه‌ای با واگرد، بازیابی anchor و focus کارت است. جزئیات اجرایی و gate در `docs/OBL11_CONTEXT_CONTINUITY_CONTRACT_FA.md` ثبت شده‌اند.

### قرارداد release gate موبایل و دسترس‌پذیری

زیر ۸۲۰px، راوی composition دسکتاپ را به surface و drawer تبدیل می‌کند. ویرایشگر یک نوار پایین دقیقاً پنج‌عملی با target ۴۴px دارد که با `visualViewport` و safe area بالای صفحه‌کلید مجازی می‌ماند؛ file open و save در مسیر پایه پنهان نمی‌شوند. native file inputها از accessibility tree کنار گذاشته می‌شوند و reduced motion فقط motion غیرضروری را هدف می‌گیرد.

state فضای کار با migration افزایشی و برگشت‌پذیر به `raavi:workspace:v2` می‌رود و هیچ کلید قدیمی یا محتوای سند را بی‌صدا حذف نمی‌کند. breakpointها، forced colors، recovery، workflow کامل و بسته‌های Web/Electron/Tauri gate خودکار دارند. قرارداد بودجه‌ها، ماتریس تست و sign-off دستی NVDA/VoiceOver در `docs/OBL12_RELEASE_GATE_FA.md` ثبت شده است.

بودجهٔ artifact تولیدی بخشی از همین Release Gate است: chunk ویرایشگر Markdown حداکثر ۳۲۰KiB، Mermaid Studio حداکثر ۱۵۰KiB و هستهٔ Graph/Cytoscape حداکثر ۴۶۰KiB است. هر build تولیدی باید نام hashed واقعی را پیدا کند و در نبود artifact یا عبور از سقف fail شود؛ افزایش سقف بدون اندازه‌گیری و تصمیم صریح طراحی مجاز نیست.

### Registration Spine

ستون میانی با دو نشان ثبت، خط ممتد و برچسب عمودی به کاربر می‌گوید دو برگ نمایش‌های هم‌زمان یک سند هستند. تمام سطح ستون، به‌جز کنترل قفل اسکرول، هدف درگ است و هنگام hover یا فوکوس یک خط آبی باریک نشان می‌دهد. رسیدن به آستانهٔ جمع‌شدن با تغییر کنترل‌شدهٔ رنگ ستون اعلام می‌شود. تقسیم‌کننده با کلیدهای جهت‌دار پنج درصد جابه‌جا می‌شود؛ Home و End پنل متناظر را جمع و Enter نسبت را ۵۰/۵۰ می‌کند. حرکت جمع‌شدن ۲۸۰ میلی‌ثانیه است و در `prefers-reduced-motion` تقریباً بی‌درنگ انجام می‌شود.

### قرارداد Product Dialog و انتخاب فرمت خروجی

خروجی DOCX و PDF در یک Product Dialog روشن و محلی نمایش داده می‌شود؛ حتی روی تم تاریک، خود Dialog از توکن‌های Light Foundation پیروی می‌کند. در دسکتاپ، backdrop فقط Workspace زیر App Chrome را می‌پوشاند و Dialog برابر ۶۰۰×۵۳۴px است: هدر و footer هرکدام ۶۴px، بدنه ۴۰۴px، شعاع ۱۴px و سایهٔ `Elevation/Dialog`. هدر تک‌ردیف است و ترتیب فیزیکی آن «بستن در چپ، عنوان در میانه، آیکن Download در راست» باقی می‌ماند.

بدنه با padding برابر ۲۴px و gap برابر ۱۴px شامل توضیح ۱۶/۳۴، عنوان «فرمت خروجی» با ۱۲/۱۸، دو Radio ردیفی ۳۶px، توضیح وابسته به فرمت و نام فایل مشتق‌شده است. DOCX ساختار قابل ویرایش را نگه می‌دارد؛ PDF خروجی ثابت A4 می‌سازد و در Windows مستقیماً ذخیره می‌شود، اما در محیط وب از مسیر چاپ استفاده می‌کند. سند اصلی، تاریخچه و annotationها با صرف انتخاب فرمت تغییر نمی‌کنند.

Radioها یک گروه نام‌دار هستند و Arrow keys انتخاب را جابه‌جا می‌کنند. Footer در لبهٔ فیزیکی چپ ابتدا عمل اصلی «خروجی DOCX/PDF» و سپس «انصراف» را قرار می‌دهد؛ Neutral border ندارد و تنها عمل اصلی ring معنایی آبی دارد. Escape، backdrop و انصراف Dialog را می‌بندند و focus به فراخوان واقعی برمی‌گردد. زیر ۸۲۰px همین سطح به Sheet پایین صفحه تبدیل می‌شود و تمام action targetها حداقل ۴۴px هستند.

چرخهٔ ساخت خروجی چهار وضعیت پیوسته دارد. `Review` تغییرهای نسخهٔ تحویلی را با شمارش و فهرست هشدارها نشان می‌دهد و ادامه فقط پس از Checkbox تأیید فعال می‌شود؛ «بازگشت» کاربر را بدون بستن Dialog به انتخاب فرمت برمی‌گرداند. `Preparing` پیشرفت determinate، مرحلهٔ جاری و تضمین محلی‌بودن پردازش را نشان می‌دهد و تا پایان عملیات با Escape، backdrop یا دکمهٔ بستن قابل خروج نیست. `Error` خطای قابل‌بازیابی و مقصد ناموفق را همراه «تلاش دوباره» و «انصراف» نگه می‌دارد. `Success` نام و مسیر واقعی فایل را نمایش می‌دهد، سند اصلی را باز نگه می‌دارد و در Windows امکان «نمایش در پوشه» دارد.

در دسکتاپ، عرض همهٔ وضعیت‌ها ۶۰۰px باقی می‌ماند و ارتفاع‌های مرجع Review، Preparing، Error و Success به‌ترتیب ۴۵۷، ۳۹۵، ۳۰۱ و ۳۷۴px هستند. سطح در همهٔ وضعیت‌ها Light-isolated است؛ Warning از توکن‌های زرد، Error از `error-soft` و Success فقط از سبز معنایی متن استفاده می‌کند. پیشرفت در Reduced Motion بدون transition نمایش داده می‌شود و Focus پس از تغییر وضعیت به اولین کنترل معنادار همان وضعیت منتقل می‌گردد.

### قرارداد پوستهٔ Settings

Settings یک صفحهٔ modal تمام‌قد و RTL است، نه پنجرهٔ کوچک تنظیمات میان‌بر. در دسکتاپ، هدر روشن ۵۶px و بدنهٔ سیال با padding افقی ۲۴px دارد؛ ناوبری راست با عرض سیال ۲۱۲ تا ۲۳۶px، gap برابر ۲۰px و محتوای اصلی `minmax(0, 1fr)` است. هیچ Section عرض ثابت ندارد و فقط محتوای اصلی مالک اسکرول عمودی است؛ پوسته، ناوبری و صفحه هرگز اسکرول افقی نمی‌سازند. ناوبری سطح `paper-50`، مرز `rule`، شعاع ۱۴px و هشت مقصد ۴۰px دارد: عمومی، ظاهر، هوش مصنوعی و گفتار، مطالعه، ویرایش، فایل‌ها و کتابخانه، حریم خصوصی و داده‌ها و میان‌برها. active state با `proof-blue-soft`، متن آبی، وزن Bold و `aria-current="page"` بیان می‌شود؛ Neutral هیچ border انتخابی ندارد.

عنوان هر دسته ۲۴/۳۴ و توضیح ۱۲/۲۰ است؛ نشان محلی‌بودن ذخیره در همان ردیف heading و با pill خنثی ۱۱/۱۸ می‌آید. Sectionها padding برابر ۱۴×۱۶px، شعاع ۱۴px و فاصلهٔ ۱۲px دارند؛ ردیف‌های معمول ۶۴px و کنترل‌های دسکتاپ حداقل ۳۶px هستند. همهٔ آیکن‌ها Google Material Symbols هجده‌پیکسلی‌اند. ArrowUp/ArrowDown میان دسته‌ها حلقوی حرکت می‌کند، Home/End ابتدا و انتها را می‌برد و Escape صفحه را می‌بندد و focus را به فراخوان واقعی برمی‌گرداند. تغییر دسته فقط surface تنظیمات را عوض می‌کند و سند یا undo history را لمس نمی‌کند.

در viewport منطقی ۱۵۳۶×۸۶۴ که معادل Full HD با مقیاس رایج ۱۲۵٪ است، تمام کنترل‌های «عمومی» باید بدون اسکرول دیده شوند. زیر ۸۲۰px، هدر ۶۴px می‌شود، دکمهٔ بازگشت target واقعی ۴۴px دارد و ناوبری به strip افقی قابل‌پیمایش در بالای محتوا تبدیل می‌شود؛ فقط بدنهٔ محتوا اسکرول عمودی دارد و خود صفحه overflow افقی نمی‌سازد. کنترل‌های هر دسته خودکار و فقط روی همان دستگاه ذخیره می‌شوند.

### قرارداد تنظیمات ظاهر و ویرایش

دستهٔ «ظاهر» سه بخش واقعی و بدون ارتفاع ثابت دارد. «حالت نمایش» سه انتخاب سیال و ۷۲px برای سیستم، روشن و تاریک نشان می‌دهد؛ انتخاب سیستم به `prefers-color-scheme` زنده گوش می‌دهد و انتخاب صریح روشن یا تاریک آن را override می‌کند. «رنگ رابط» ۱۳ رنگ تأییدشدهٔ Figma دارد: آبی راوی `#2557e5`، فیروزه‌ای `#007a7a`، جنگلی `#277a4b`، زیتونی `#647117`، کهربایی `#9a5a00`، نارنجی `#a9470b`، آجری `#9f3f32`، مرجانی `#a63e53`، تمشکی `#9e2f63`، بنفش `#75419a`، نیلی `#4656a6`، گردویی `#75523b` و گرافیتی `#52616b`. swatch انتخاب‌شده سطح ۴۴px با شعاع ۱۲px و علامت سفید دارد. انتخاب رنگ باید توکن‌های عمل اصلی، لینک، focus و active state را در کل محصول تغییر دهد؛ رنگ‌های معنایی خطا، هشدار و موفقیت ثابت می‌مانند.

«حرکت و انیمیشن» سه انتخاب معمولی، پیروی از سیستم و کم‌شده دارد. حالت سیستم از `prefers-reduced-motion` پیروی می‌کند؛ معمولی ترجیح سیستم را برای حرکت‌های غیرضروری override می‌کند و کم‌شده همهٔ transition و animationهای غیرضروری را تقریباً آنی می‌سازد. هر سه ترجیح ظاهر در `localStorage` همان دستگاه ذخیره و پیش از نخستین paint روی root اعمال می‌شوند تا پرش تم رخ ندهد. در موبایل کارت‌ها پشته‌ای، swatchها wrap و همهٔ targetها حداقل ۴۴px هستند.

دستهٔ «ویرایش و نمای کد» pill ذخیرهٔ تکراری ندارد و یک Section دقیق ۱۰۴۰×۳۲۲px می‌سازد. کنترل‌های واقعی در ستون مرکزی ۸۲۰px شامل دو ردیف ۸۸px هستند: نمایش تولبار پایین و جهت پیش‌فرض خودکار/RTL/LTR. خاموش‌کردن تولبار، Command Palette و میان‌برهای استاندارد فایل را حذف نمی‌کند؛ منوی `/` فقط متعلق به ویرایش روان است و در نمای کد وجود ندارد. انتخاب جهت فقط projection نمای کد را تغییر می‌دهد؛ هیچ LRM، RLM یا نویسهٔ مخفی به Markdown افزوده نمی‌شود. هر تغییر بلافاصله روی نمونهٔ CodeMirror موجود اثر می‌گذارد، محلی persist می‌شود و محتوای سند یا Undo history را لمس نمی‌کند.

### قرارداد تنظیمات مطالعه

دستهٔ «مطالعه» در دسکتاپ دو Section سیال و هم‌تراز کنار هم دارد؛ هر Section سه ردیف ۶۴px می‌سازد. «متن و چیدمان» سه کنترل segmented با ابعاد حداکثر ۲۷۰×۴۰px دارد: اندازهٔ متن کوچک/معمولی/بزرگ، فاصلهٔ خطوط فشرده/معمولی/باز و عرض متن باریک/متعادل/عریض. مقادیر مصوب آن‌ها به‌ترتیب ۱۶/۱۸/۲۰px، line-height برابر ۱٫۷/۲/۲٫۲۵ و عرض سند برابر ۶۴۰/۷۶۰/۸۸۰px است. این تنظیم‌ها فقط projection خواندن را تغییر می‌دهند و Markdown و Undo history را دست‌نخورده نگه می‌دارند.

«رفتار مطالعه» سه Switch واقعی برای یادآوری موقعیت هر فایل، پنهان‌شدن خودکار هدر و بازشدن فهرست مطالب هنگام ورود به سند ساختاری دارد. پیش‌فرض فهرست خاموش است تا Reading مطابق قرارداد rail-only باز شود؛ فعال‌کردن آن در دسکتاپ پنل عادی فهرست را باز می‌کند و در موبایل Drawer را خودکار تحمیل نمی‌کند. خاموش‌کردن یادآوری، رکوردهای قبلی را حذف نمی‌کند اما از خواندن یا نوشتن موقعیت جلوگیری می‌کند. همهٔ ترجیح‌ها محلی، امن در برابر دادهٔ خراب و مستقل از محتوای سند ذخیره می‌شوند. Radioها با Arrow/Home/End و Switchها با Space/Enter کار می‌کنند؛ در موبایل همهٔ targetها حداقل ۴۴px و سطح بدون overflow افقی است.

### قرارداد تنظیمات فایل‌ها و کتابخانه

دستهٔ «فایل‌ها و کتابخانه» از Sectionهای سیال، headingهای محتوایی و ردیف‌های ۶۴px Settings استفاده می‌کند. این دسته به‌دلیل اتصال ابری، سیاست بکاپ و پوشه‌های متصل می‌تواند تنها اسکرول عمودی محتوای اصلی را مصرف کند، اما اسکرول افقی یا اسکرول داخلی جداگانه ندارد. فایل موجود به‌صورت پیش‌فرض در «مطالعه» باز می‌شود و کاربر می‌تواند آن را به «نوشتن» تغییر دهد؛ سند تازه مستقل از این ترجیح همیشه در حالت نوشتن ساخته می‌شود. انتخاب نمایش کتابخانه فقط projection کاوشگر را فیلتر می‌کند و فایل روی دیسک، سند باز و نمایهٔ محلی را تغییر نمی‌دهد.

به‌روزرسانی خودکار، subscription تغییرات بیرونی پوشه را کنترل می‌کند؛ خاموش‌کردن آن مسیر «به‌روزرسانی» دستی را نگه می‌دارد. بخش «پوشه‌های متصل» شمارش واقعی پوشه و فایل، افزودن پوشه، refresh و قطع اتصال دو‌مرحله‌ای inline دارد. قطع اتصال مجوز و watcher همان root را در Electron/Tauri و handle همان root را در Web کنار می‌گذارد، اما هیچ فایل یا سند بازی را حذف نمی‌کند. نام و مسیر با جهت طبیعی و bidi ایزوله نمایش داده می‌شوند، لیست فشرده است و آیکن تکراری تزئینی ندارد. همهٔ ترجیح‌ها فقط در `localStorage` همان دستگاه ذخیره می‌شوند؛ زیر ۸۲۰px actionها و Radio/Switchها target حداقل ۴۴px دارند و surface overflow افقی نمی‌سازد.

«بازیابی روی این دستگاه» پس از اتصال Provider، فهرست واقعی Vault همان حساب را دریافت می‌کند و همهٔ اسناد دارای متن را به‌صورت پیش‌فرض انتخاب می‌کند. انتخاب مقصد فقط پوشهٔ والد را تعیین می‌کند؛ راوی همیشه یک پوشهٔ تازه با تاریخ بازیابی می‌سازد و هیچ فایل موجودی را بازنویسی نمی‌کند. محتوای نهایی به Markdown استاندارد برمی‌گردد، Annotationها در بلوک پنهان Markdown، نسخه‌ها در تاریخچهٔ محلی و تصاویر/صوت/پیوست‌ها در پوشه‌های نسبی کنار سند بازسازی می‌شوند. نتیجه به کتابخانه افزوده و مسیر آن قابل نمایش است. لغو انتخاب مقصد هیچ اثری ندارد؛ شکست یک سند مانع حفظ سندهای موفق نمی‌شود و شکست کامل با پیام بازیابی‌پذیر و بدون تغییر فایل‌های محلی پایان می‌یابد. حالت‌های disconnected، loading، empty، selection، restoring، partial success، success و error باید با متن صریح و `aria-live` مناسب پوشش داده شوند.

### قرارداد تنظیمات حریم خصوصی و داده‌ها

دستهٔ «حریم خصوصی و داده‌ها» سه Section مستقل دارد. «تصاویر بیرونی» سه انتخاب سیال و ۶۸px برای «همیشه مسدود»، «هر بار بپرس» و «همیشه بارگیری» نشان می‌دهد؛ پیش‌فرض «هر بار بپرس» است. در دو حالت نخست تا پیش از اجازهٔ واقعی کاربر هیچ عنصر تصویر با URL بیرونی ساخته و هیچ درخواست شبکه‌ای فرستاده نمی‌شود. اجازهٔ موردی فقط در همان نشست معتبر است، در فایل یا Undo history ذخیره نمی‌شود و تصویر داخلی یا مسیر محلی را تحت تأثیر قرار نمی‌دهد.

«لینک‌ها» یک Switch پیش‌فرض روشن دارد. کلیک مقصد `http/https` پیش از خروج، Dialog قابل‌دسترس شامل نشانی کامل، انصراف و بازکردن نشان می‌دهد؛ در حالت خاموش همان مقصد از adapter مشترک Electron/Tauri یا پنجرهٔ امن Web باز می‌شود. مقصد نسبی و داخلی از این مسیر عبور نمی‌کند. «داده‌های این دستگاه» دو عمل دو‌مرحله‌ای inline دارد: پاک‌کردن Recent باید فقط فهرست بازشدن‌ها و ranking وابسته را در storage واقعی runtime حذف کند، و بازنشانی تنظیمات فقط کلیدهای تنظیم و workspace را به پیش‌فرض برگرداند. هیچ‌کدام حق حذف فایل، سند باز، snapshot سند، annotation، نسخه یا دارایی `.ravi` را ندارند.

همهٔ انتخاب‌ها امن در برابر دادهٔ خراب، محلی و بدون telemetry هستند. Radioها Arrow/Home/End، Switch با Space/Enter و تأییدها focus انتقالی و بازگشت focus دارند. زیر ۸۲۰px کارت‌ها پشته‌ای و تمام actionها حداقل ۴۴px هستند و Settings overflow افقی نمی‌سازد.

### قرارداد تنظیمات میان‌برها

دستهٔ «میان‌برها» فهرست کامل و قابل‌جست‌وجوی میان‌برهای فعال را نمایش می‌دهد. منبع اصلی، `COMMAND_REGISTRY` و bindingهای فعال همان پلتفرم/سطح است؛ رفتارهای زمینه‌ای ویرایشگر که فرمان مستقل ندارند از `SHORTCUT_GUIDE_SECTIONS` ادغام و موارد تکراری حذف می‌شوند. بالای فهرست یک جست‌وجوی sticky با شمارندهٔ نتیجه قرار دارد و عنوان، توضیح، زمینه، کلیدواژه، گروه و متن binding را جست‌وجو می‌کند.

بدنه یک فهرست عمودی گروه‌بندی‌شده است: نام و توضیح فرمان در سمت راست و ترکیب کلیدها در سمت چپ فیزیکی. هر کلید داخل `<kbd>` مستقل ۲۸px، هر chord بدون شکست و شکست خط فقط میان chordها یا گزینه‌های جایگزین انجام می‌شود. متن و keycapها از پلتفرم واقعی مشتق می‌شوند: Windows/Linux از Ctrl و Alt و macOS از ⌘ و Option استفاده می‌کند. شمارندهٔ نتیجه با `aria-live`، ورودی دارای label واقعی و وضعیت بدون نتیجه دارای راه بازگشت به کل فهرست است.

در نمایش کوچک، Sectionها تمام‌عرض و هر ردیف بدون overflow به copy و keycapهای پشته‌ای تبدیل می‌شود. search و actionها حداقل ۴۴px هستند و هیچ اسکرول افقی ایجاد نمی‌شود. ترتیب خواندن معنایی، نام پلتفرم در accessibility tree، focus دسته، Escape و بازگشت focus از قرارداد پوستهٔ Settings پیروی می‌کنند و بازکردن این صفحه سند یا Undo history را تغییر نمی‌دهد.

### قرارداد New Tab و قالب‌های فارسی

صفحهٔ New Tab از صفحهٔ جدیدتر `59 Screens / New Tab`، فریم S01 و برای App Chrome، Tab Bar و Rail از `51 Screens / Notes Workspace P0` پیروی می‌کند. در قاب ۱۲۸۰×۹۱۴، Title/Command Bar برابر ۹۲px، Tab Bar برابر ۳۶px و Rail هفت‌گانه برابر ۵۶px است. Tab Bar پس از بازشدن سند نیز حفظ می‌شود و تمام مختصات Stage در W03 تا W10 و P01 بر مبنای همین inset سی‌وشش‌پیکسلی‌اند. بازکردن New Tab از Reading ابتدا Reading را ترک می‌کند؛ یک تب واقعی و انتخاب‌شده با عنوان «تب جدید» می‌سازد و دکمهٔ `+` جدا و borderless می‌ماند.

سطح قالب‌ها در Stage برابر ۱۲۲۴×۷۸۶px و کاملاً RTL است. محتوای اصلی یک شبکهٔ ۷۶۰px وسط‌چین دارد: header در y=166 با ارتفاع ۵۸px، کارت دفتر ۲۸۰×۵۸px در چپ فیزیکی، عنوان و راهنما در راست، شبکه در y=252 با ۱۲ کارت ۳۷۲×۶۸px، gap افقی ۱۶px و عمودی ۱۰px، و راهنمای میان‌بر در y=760 است. کارت نخست «یادداشت خالی» focus اولیه می‌گیرد؛ کارت‌ها با ترتیب فیزیکی مرجع، متن راست‌چین و icon slot در چپ نمایش داده می‌شوند. Neutral از `paper-50/rule` و Focus از `proof-blue-soft/focus-ring` استفاده می‌کند.

Arrowها بر اساس موقعیت فیزیکی شبکه، Home/End ابتدا و انتها و Enter/Space قالب را انتخاب می‌کنند. فقط یک کارت `tabIndex=0` دارد. تغییر دفتر از کارت مستقل محلی انجام می‌شود و «نام و نوع سفارشی» همان مسیر Dialog موجود را باز می‌کند. زیر ۸۲۰px header و دفتر پشته‌ای، شبکه تک‌ستونه و همهٔ actionها حداقل ۴۴px هستند؛ RTL، bidi مسیر پوشه و نبود overflow افقی باید حفظ شود.

### قرارداد Launch برای کاربر بازگشتی

Launch بازگشتی یک سطح مستقل و بدون App Chrome است. در قاب ۱۴۴۰×۱۰۲۴، کل viewport سطح `paper-200` با مرز `rule-strong` و شعاع ۱۸px است؛ padding بالا ۵۴px، افقی ۱۲۰px و پایین ۴۸px دارد. heading برابر ۱۲۰۰×۱۰۰px است: برند RAAVI، عنوان «خوش آمدید» با ۳۰/۴۷ و توضیح ۱۴/۲۲ در راست فیزیکی؛ شمارش واقعی حداکثر ۹ Recent و متن مرتب‌سازی در چپ فیزیکی قرار می‌گیرند.

Recentها فقط از تاریخچهٔ محلی همان runtime، بدون دادهٔ نمونه یا درخواست شبکه، بر اساس آخرین زمان بازشدن مرتب می‌شوند. شبکهٔ دسکتاپ ۱۲۰۰×۷۳۲px و ۳×۳ است؛ کارت‌ها ۳۸۴×۲۳۲px، gap افقی ۲۴px، gap عمودی ۱۸px و شعاع ۱۴px دارند. preview برابر ۱۴۲px و metadata برابر ۹۰px است. کارت focused/hovered مرز ۲px و preview آبی نرم می‌گیرد و عمل «باز کردن» را آشکار می‌کند؛ Neutral مرز `rule` یک‌پیکسلی دارد. نام، پسوند، پوشه و زمان از دادهٔ واقعی فایل می‌آیند و مسیرهای ترکیبی با bidi isolate نمایش داده می‌شوند.

کارت نخست هنگام ورود focus می‌گیرد. ArrowLeft/ArrowRight در ردیف، ArrowUp/ArrowDown میان ردیف‌ها و Home/End ابتدا و انتهای شبکه را انتخاب می‌کنند؛ Enter یا Space فایل را از adapter مشترک Web/Electron/Tauri باز می‌کند. فایل گم‌شده از Recent محلی حذف و خطای بازیابی روی خود Launch اعلام می‌شود. در نبود Recent، مسیر قالب‌های محلی موجود حفظ می‌شود و در نبود دفتر، انتخاب پوشه پیش‌نیاز باقی می‌ماند. زیر ۱۱۰۰px شبکه دو‌ستونه و زیر ۸۲۰px تک‌ستونه، تمام‌عرض و با action target حداقل ۴۴px است؛ Launch نباید overflow افقی یا دسترسی keyboard به chrome پنهان ایجاد کند.

### Graph Studio

P21 از فریم‌های G01 تا G11 صفحهٔ `Screens / Graph` پیروی می‌کند. Graph Studio یک
لایهٔ تمام‌صفحهٔ موقت است و سند زیر آن از DOM حذف نمی‌شود. در قاب ۱۳۶۶×۷۶۸، هدر
روشن و borderless آن دقیقاً ۷۲px است: هویت «استودیو گراف / نام سند · نمودار تازه»
در راست، وضعیت نقطه‌ای «انتخاب نوع / آماده / در حال ساخت / نیاز به اصلاح /
تمام‌صفحه» در مرکز و دو عمل ۱۲۸×۳۶ و ۱۲۴×۳۶ در چپ قرار می‌گیرند. سطح کار از
Preview چپ ۸۳۸px، Divider قابل‌درگ ۸px و Builder راست ۵۲۰px تشکیل می‌شود؛ هر Pane
Header برابر ۵۲px است. سطوح خنثی border ندارند و فقط field، card انتخاب و focus
معنایی stroke می‌گیرند.

در Type Selection، جست‌وجو تمام ۲۹ ساختار محلی را پوشش می‌دهد، اما حالت اولیه فقط
هشت پیشنهاد «فرایند، توالی، کلاس، حالت، معماری، رابطهٔ موجودیت، گانت و نقشهٔ
ذهنی» را در شبکهٔ ۲×۴ نشان می‌دهد. هر کارت ۲۳۰×۸۸px، شعاع ۶px، icon slot برابر
۴۴px و تایپ Label/Caption برابر ۱۲/۱۸ و ۱۱/۱۷ دارد. انتخاب نوع، Builder فارسی
موجود را باز می‌کند؛ حالت Easy و Advanced روی همان draft و همان Undo history کار
می‌کنند و Samples یک پنل محلی ۵۲۰px است، نه ستون دائمی یا منبع دوم داده.

Canvas تخت `paper-100` است و grid یا سایهٔ تزئینی ندارد. ابزار دست پیش‌فرض در
گوشهٔ پایین‌چپ فعال است؛ درگ از نمودار یا فضای خالی Pan می‌کند و Zoom out، درصد،
Zoom in، Fit و Fullscreen در Pane Header می‌مانند. خطای Mermaid داخل خود Canvas
در کارت محلی ۶۰۰×۲۱۸px با پیام فارسی، پیشنهاد اصلاح، رفتن به ردیف، نمونهٔ صحیح و
جزئیات فنی ظاهر می‌شود و هدر یا کل میز را به Banner تبدیل نمی‌کند. حالت Loading،
Empty و آخرین SVG سالم نیز همین ناحیه را مصرف می‌کنند. خروج از draft تغییرکرده،
dialog برابر ۴۴۰×۲۱۴px با متن «تغییرات کنار گذاشته شوند؟» باز می‌کند و پاک‌کردن
draft فقط پس از تأیید انجام می‌شود.

در قاب Compact Windows برابر ۸۲۰×۹۸۰، هدر ۱۱۲px و دو ردیفی است؛ Modeها و اعمال
سند target برابر ۴۴px دارند. Builder از y=112 تا y=562، Divider افقی ۸px و Preview
از y=570 تا پایین قاب قرار می‌گیرند. هیچ Pane به overlay تبدیل نمی‌شود، focus و
Caret هنگام تغییر Mode حفظ می‌شوند و reduced motion تمام چرخش‌های غیرضروری را
متوقف می‌کند.

نمودار در Reading دقیقاً از Preview تخت و بدون border یا خط تزئینی R09 پیروی
می‌کند و کنترل «تمام‌صفحه» همان SVG رندرشده را بدون ساخت نسخهٔ دوم وارد Graph
Viewer می‌کند. Canvas این Viewer طبق R10 یک سطح تخت `editor-surface-bg` است و
toolbar چهارتایی آن در بالا-چپ می‌ماند؛ هیچ grid، درصد دیداری یا Dock پایین‌وسطی
در Reading وجود ندارد. نمودار مستقیماً فضای بزرگ Canvas را با inset امن مصرف
می‌کند؛ کارت کوچک Preview، metadata تکراری یا action دوم Fullscreen داخل Canvas
مجاز نیست. Viewer با Escape یا «بازگشت به سند» بسته می‌شود. در میز
کار کنترل ویرایش حفظ می‌شود و در چاپ، کنترل‌ها حذف و SVG برداری با
`break-inside: avoid` نگه‌داری می‌شود.

### Formula Studio

P22 از صفحهٔ `47 Screens / Formula Studio` و فریم‌های Desktop/Empty،
Desktop/Building، Compact/Building و State Gallery پیروی می‌کند. انتخاب نوع
«فرمول» از Block Menu یا Slash Menu یک لایهٔ تمام‌صفحهٔ مستقل باز می‌کند؛ بلوک
خام `$$` نباید پیش از تأیید کاربر وارد سند شود. در قاب ۱۳۶۶×۷۶۸، App bar دقیقاً
۶۸px و ribbon دسته‌ها ۷۲px است. هویت «استودیو فرمول / بلوک مستقل» در راست،
اعمال سند و وضعیت ذخیره در چپ و دسته‌های «پایه، توان و ریشه، حسابان، ماتریس و
نمادها» در ribbon قرار می‌گیرند.

سطح کار از بوم دیداری چپ ۱۰۳۰px و palette راست ۳۳۶px تشکیل می‌شود. palette شش
ساختار کسر، توان، ریشه، انتگرال، ماتریس و نمادها را در شبکهٔ ۲×۳ با tileهای
۱۳۲×۱۱۲px نشان می‌دهد. آبی فقط برای ساختار انتخاب‌شده، slot فعال، focus و عمل
اصلی مصرف می‌شود. سطح خنثی borderless می‌ماند و مرز فقط stage، field، tile و
slot معنایی را تعریف می‌کند. ورودی مستقیم، تبدیل‌های سادهٔ توان/ریشه/نماد را
محلی انجام می‌دهد و خروجی همیشه یک Display Math Block مستقل با delimiterهای
`$$` است؛ هیچ شبکه یا درخواست بیرونی در ساخت یا رندر فرمول دخیل نیست.

هر ساختار مجموعهٔ slotهای واقعی دارد. Tab/Shift+Tab و کلیدهای جهت میان slotها
حرکت می‌کنند، focus و مقدار slot حفظ می‌شود و IME فارسی shortcut ناخواسته اجرا
نمی‌کند. Undo/Redo هر تغییر ساختاری یا مقدار slot را مستقل برمی‌گرداند. دکمهٔ
افزودن تا کامل‌شدن همهٔ slotهای لازم غیرفعال است؛ خطا کنار بوم و با مسیر بازیابی
فارسی نمایش داده می‌شود. draft تغییرکرده هر ۴۵۰ms در storage محلی همان دستگاه
حفظ می‌شود و خروج، تأیید «تغییرات کنار گذاشته شوند؟» با focus trap و بازگشت focus
دارد. ویرایش بلوک موجود با تطبیق raw block انجام می‌شود و در تعارض، سند بیرونی
بازنویسی نمی‌شود.

پس از تأیید، نمای Writing خود Display Math رندرشده را در یک بلاک مستقل نشان
می‌دهد و delimiter یا LaTeX خام را آشکار نمی‌کند. عمل «ویرایش در استودیو» و
double-click روی همان بلاک، Studio را با همان فرمول باز می‌کنند؛ منبع `$$…$$`
فقط در نمای Code قابل مشاهده و ویرایش مستقیم است. رندر Writing، Reading و
Save/Open همگی از همان LaTeX ذخیره‌شده استفاده می‌کنند و نسخهٔ موازی نمی‌سازند.

در قاب ۸۲۰×۹۸۰، App bar برابر ۷۲px و Touch ribbon برابر ۶۴px است؛ بوم از y=136
تا y=560 و palette به‌صورت sheet عادی و غیر-overlay از y=560 تا پایین قاب قرار
می‌گیرد. actionها و slotها target حداقل ۴۴px دارند، شبکهٔ palette سه‌ستونه است و
در عرض تلفن به دو ستون تبدیل می‌شود. هر پنج دسته در ribbon باقی می‌مانند؛ tabها
roving tabindex و Arrow/Home/End دارند و «مشاهدهٔ همه» فیلتر و دسته را به نمای
کامل بازمی‌گرداند. reduced motion بازشدن Studio را بی‌حرکت می‌کند و ترتیب
خواندن، نام فرمول و وضعیت slotها برای screen reader اعلام می‌شود.

P27 مدل دیداری فرمول را به یک Expression Tree واقعی ارتقا می‌دهد: هر ساختار در
slot فعال nest می‌شود و Breadcrumb و Context Inspector مسیر و node فعال را بدون
modal جداگانه نشان می‌دهند. Matrix Mode ابعاد ۱×۱ تا ۸×۸ و شش delimiter، و
Calculus Mode انتگرال‌های تکی/دوگانه/سه‌گانه/مسیر بسته، مشتق معمولی/جزئی، مجموع،
حاصل‌ضرب و حد را با کران، مرتبه و متغیر قابل‌ویرایش پوشش می‌دهد. Symbol Picker
در همان palette، گروه‌های یونانی، عملگرها، روابط، مجموعه‌ها و پیکان‌ها را با
جست‌وجوی فارسی، انگلیسی و LaTeX ارائه می‌کند؛ Recent Symbols حداکثر هشت مورد و
فقط محلی است. درج هر نماد، resize یا تغییر عملگر دقیقاً یک Undo transaction
می‌سازد، focus منطقی را حفظ می‌کند و serialize/parse باید همان LaTeX canonical
را در Save/Open بازیابی کند. Advanced LaTeX فقط مسیر long-tail است و ورودی
نامعتبر یا ناامن را پیش از ثبت با مکان خطا رد می‌کند.

در دستهٔ «توابع و جبر»، یک Function Node با ۱۸ تابع مثلثاتی، معکوس، هذلولوی،
لگاریتمی و جبری، یک تا سه آرگومان و زیرنویس اختیاری ساخته می‌شود. Fence Node
هشت جفت پرانتز، براکت، آکولاد، قدرمطلق، نُرم، زاویه، کف و سقف را با حفظ عبارت
داخلی عوض می‌کند. Cases بین یک تا هشت ردیف «مقدار · شرط» دارد؛ Accent هشت
نشانهٔ hat، bar، vector، dot، double-dot، tilde، overline و underline را پوشش
می‌دهد و Binomial دو slot مستقل بالا و پایین دارد. همهٔ این nodeها می‌توانند در
slot یکدیگر nest شوند و تغییر نوع، تعداد آرگومان/ردیف یا نشانه هرکدام فقط یک
Undo transaction است.

زبان خود بوم ریاضی همیشه لاتین/نمادین و `dir=ltr` است؛ واژهٔ فارسی داخل عبارت،
slot یا جداکنندهٔ Cases نمایش داده نمی‌شود. بنابراین جداکنندهٔ دیداری هر ردیف
Cases برابر `if` است و فارسی فقط در label، راهنما و Inspector بیرون عبارت باقی
می‌ماند.

Release Gate فرمول با corpus حداقل ۱۰۰ فرمول دانشگاهی اجرا می‌شود و باید برای
هر نمونه اعتبارسنجی، parse/serialize، ذخیرهٔ محلی و بازگشایی دقیق را پاس کند.
مسیر کامل ساخت، Copy/Paste، Undo/Redo و تأیید نیز فقط با کیبورد در دسترس است؛
Paste ساختاری فقط LaTeX معتبر را به یک Expression Tree قابل‌ویرایش تبدیل می‌کند.

## Do's and Don'ts

### Do:

- **Do** فضای متن را از رابط بیشتر نگه دار.
- **Do** از علائم چاپی برای وضعیت، مرز و جهت استفاده کن.
- **Do** قطعه‌کد را LTR و با پیمایش امن در نمایشگر کوچک نمایش بده.
- **Do** آبی را برای عمل و فوکوس ذخیره کن.

### Don't:

- **Don't** نوشته را داخل مجموعه‌ای از کارت‌های هم‌اندازه خرد کن.
- **Don't** کاغذ را با بافت تزئینی یا نویز مصنوعی پر کن.
- **Don't** برای ظاهر فنی از تک‌عرض‌کردن متن فارسی استفاده کن.
- **Don't** روی سطح کاری تخت سایه و خط مرزی را هم‌زمان انباشته کن.
